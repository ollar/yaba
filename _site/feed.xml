<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ru"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" hreflang="ru" /><updated>2026-01-09T11:34:32+03:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Your awesome title</title><subtitle>Write an awesome description for your new site here. You can edit this line in _config.yml. It will appear in your document head meta (for Google search results) and in your feed.xml site description.</subtitle><entry><title type="html">Собираем Linux, который весит меньше, чем мем с котиком, ну или почти…</title><link href="http://localhost:4000/habr/2025/12/03/build-micro-linux.html" rel="alternate" type="text/html" title="Собираем Linux, который весит меньше, чем мем с котиком, ну или почти…" /><published>2025-12-03T00:00:00+03:00</published><updated>2025-12-03T00:00:00+03:00</updated><id>http://localhost:4000/habr/2025/12/03/build-micro-linux</id><content type="html" xml:base="http://localhost:4000/habr/2025/12/03/build-micro-linux.html"><![CDATA[<p>Мне всегда было интересно, насколько минимальным может быть Linux.</p>

<p>Три года назад я уже публиковал <a href="https://habr.com/ru/companies/ruvds/articles/702570/">статью</a>, но понимание темы меняется. Сейчас хочу освежить знания и поделиться ими.</p>

<p>В отличие от предыдущей статьи, в этой я рассматриваю Linux с более свежим ядром с минимальной конфигурацией на основе tinyconfig, оформленный в виде одного файла и загружаемый при помощи UEFI.</p>

<p>Зачем вообще нужно создавать такие сборки Linux?</p>

<p>Установил готовый дистрибутив или загрузился в Live — работай и изучай. Но такой подход не даёт настоящей уверенности в знаниях. Представьте: водитель и шофёр. Водитель просто ездит. Шофёр знает устройство автомобиля и может его починить.</p>

<p>Современные автомобили сложны — починить всё самому уже нельзя. Но вы можете открыть капот, измерить уровень масла, заменить колесо в случае прокола, установить винт для буксировочного троса и т. д. Так же и с Linux: коммерческий дистрибутив вы вряд ли соберёте, но на одну ступеньку вверх по лестнице профессионализма подниметесь.</p>

<p>На мой взгляд, описание создания минимального Linux не должно быть большим, поэтому я старался его сделать минимально возможным, вложив максимум смысла.</p>

<p>Как минимальный Linux служит для понимания более сложных вещей, так и моя статья даёт базу для дальнейшего более глубокого изучения.</p>

<p>В комментариях к моей предыдущей статье я видел замечания, что можно использовать <a href="https://buildroot.org/">buildroot</a> или книгу <a href="https://www.linuxfromscratch.org/">Linux From Scratch</a>. Скажу только, что я преследую другие цели: собрать минимальный Linux без использования специализированных программ и сделать это как можно проще.</p>

<p>Всем, кого заинтересовало, добро пожаловать под кат.</p>

<p>Действия, описанные в статье, также приведены в виде небольшого bash-скрипта в <a href="https://github.com/artyomsoft/minimal-uefi-linux">моём репозитории на GitHub</a>.</p>

<h3 id="архитектура">Архитектура</h3>

<p>Начнём с того, что необходимо для минимального Linux.</p>

<p>Сначала нам нужно определиться с <em>платформой</em>, на которой нужно запустить Linux. <em>Платформа</em> — это набор программно-аппаратных средств, на которых запускается Linux. Программный код ядра Linux общий для всех платформ, но часть кода является платформенно-зависимым.</p>

<p>Нам нужно:</p>

<ul>
  <li>
    <p><em>ядро</em>, которое содержит только самое необходимое;</p>
  </li>
  <li>
    <p><em>корневая файловая система</em>;</p>
  </li>
  <li>
    <p>минимальный набор пользовательских программ и библиотек в корневой файловой системе;</p>
  </li>
  <li>
    <p>нечто, что может запустить само ядро на платформе (загрузчик).</p>
  </li>
</ul>

<h4 id="платформа">Платформа</h4>

<p>Существует множество платформ, на которых можно запустить Linux. Мы выбираем ту, которая сейчас есть практически у всех — <em>UEFI x86_64</em>. Это ноутбук или стационарный компьютер, где в качестве Firmware установлен UEFI. Скорее это не платформа, а семейство платформ, так как компьютеры и ноутбуки отличаются аппаратной начинкой. В нашем случае это не имеет значения, так как мы будем использовать минимальные возможности. Для тестирования Linux удобно использовать <em>QEMU</em>. <em>QEMU</em> — это система виртуализации, поддерживающая эмуляцию множества платформ, которая и сама может также рассматриваться как платформа.</p>

<h4 id="ядро">Ядро</h4>

<p><em>Ядро</em> — это файл в простейшем случае или набор файлов в более сложных случаях. Мы создаём минимальный Linux, поэтому я рассматриваю только первый случай. Как я говорил ранее, бо́льшая часть исходного кода ядра Linux одинаковая для всех платформ, но бинарный код ядра будет отличаться из-за <em>архитектуры системы команд</em> (ISA) выбранной платформы, аппаратных средств выбранной платформы и специфики загрузки ядра на выбранной платформе.</p>

<h4 id="корневая-файловая-система">Корневая файловая система</h4>

<p><em>Корневая файловая система</em> — это структура данных, где находится набор пользовательских программ, файлы устройств, другие служебные файловые системы, используемые ядром. Корневая файловая система служит связующим звеном между пространством ядра и пространством пользователя.</p>

<h4 id="набор-пользовательских-программ-и-библиотек">Набор пользовательских программ и библиотек</h4>

<p>Другим звеном являются <em>системные вызовы</em> — особые функции, исполняемые в <em>пространстве ядра</em>, которые можно вызывать из <em>пространства пользователя</em>.</p>

<p>Обычно программисты вызывают не их, а обёртки для них, которые находятся в <em>стандартной библиотеке (libc)</em>. В настоящее время наибольшее распространение получили две: <em>glibc</em> и <em>musl</em>. Первая, как правило, используется для десктопных и серверных систем, вторая — для встраиваемых и минималистичных (например, Alpine Linux — минималистичная Linux, предназначенная для запуска в Docker-контейнерах). Кроме стандартной библиотеки, существуют и другие библиотеки, предназначенные для решения специфичных задач.</p>

<p>Приложения могут:</p>

<ul>
  <li>
    <p>включать <em>объектный код</em> используемых функций (<em>статическая линковка</em>) в себя;</p>
  </li>
  <li>
    <p>хранить только ссылку на используемые библиотеки и использовать совместно с другими приложениями (<em>разделяемые библиотеки</em>, <em>динамическая линковка</em>).</p>
  </li>
</ul>

<p>С целью упрощения мы будем использовать статическую линковку.</p>

<p>Набор пользовательских приложений для Linux громаден. При общем изучении Linux используют стандартные приложения, которые являются аналогами команд из операционной системы Unix.</p>

<p>Это пакеты программ: <em>coreutils</em>, <em>net-tools</em>, <em>util-linux</em>, <em>procps</em>, и др. Но для минималистичных Linux существует проект <em>BusyBox</em>, который содержит большинство аналогов программ из ранее перечисленных пакетов.</p>

<p>Из- за того, что он минималистичный, некоторые команды являются упрощёнными и поддерживают не все опции. Особенностью BusyBox является то, что для всех команд используется один и тот же исполняемый файл. А его имя или имя ссылки на него определяют конкретную команду, которую мы хотим запустить.</p>

<h4 id="система-инициализации">Система инициализации</h4>

<p>Для понимания процесса загрузки Linux важно понимание роли <em>системы инициализации</em>. При загрузке Linux наступает момент, когда ядро готово запустить первый процесс в пользовательском пространстве.</p>

<p>Имя файла, с которого запускается этот процесс, имеет имя (<em>init</em>), а процесс — особый идентификатор (<em>PID</em> равный 1). Файл init должен располагаться по определённому пути, где его сможет найти ядро.</p>

<p>Теоретически <code class="language-plaintext highlighter-rouge">init</code> может делать что угодно, но основная его задача — запустить <em>фоновые процессы</em> в пространстве пользователя и проинициализировать другие компоненты Linux. Такой init-процесс называется <em>системой инициализации</em>. Для Linux разработаны несколько таких систем:</p>

<ul>
  <li>
    <p>System V init — классическая UNIX-подобная система инициализации;</p>
  </li>
  <li>
    <p>systemd — современная, широко используемая система инициализации;</p>
  </li>
  <li>
    <p>OpenRC — альтернативная система инициализации, основанная на скриптах;</p>
  </li>
  <li>
    <p>BusyBox init — минималистичная система инициализации для встраиваемых систем.</p>
  </li>
</ul>

<p>Но мы создадим свою и уместим её в bash-скрипт.</p>

<h4 id="корневая-файловая-система-и-initramfs">Корневая файловая система и initramfs</h4>

<p>Загрузка Linux возможна как с использованием <em>промежуточной корневой файловой системы</em>, служащей для первоначальной инициализации пространства пользователя так и без неё. Промежуточная корневая файловая система называется <em>initramfs</em>. В этом случае необходимо при загрузке Linux указать как имя файла образа ядра, так и имя файла образа initramfs. Физически файл образа initramfs — это сжатый или несжатый архив формата <em>cpio</em>.</p>

<p>Возможен вариант, когда образ initramfs внедряется в файл образа ядра, мы воспользуемся этой возможностью для упрощения и получения wow-эффекта.</p>

<p>Программы и модули, находящиеся в initramfs, служат для того, чтобы подготовить ядро к переключению на постоянную корневую файловую систему. То есть если в файл ядра включить необходимые модули, то возможно монтирование корневой файловой системы и запуск init-процесса без использования initramfs, но в случае минимальной конфигурации Linux это усложняет подготовку загрузочного образа, так как нужно:</p>

<ul>
  <li>
    <p>разметить диск;</p>
  </li>
  <li>
    <p>записать в один из разделов образ корневой файловой системы;</p>
  </li>
  <li>
    <p>добавить загрузчик операционной системы, в случае традиционной загрузки при помощи GRUB;</p>
  </li>
  <li>
    <p>добавить и отформатировать <em>ESP-раздел</em> в случае непосредственной загрузки при помощи UEFI.</p>
  </li>
</ul>

<p>Поэтому мы разместим корневую файловую систему в файле образа initramfs, а файл образа initramfs включим файл образа ядра Linux.</p>

<p>Чтобы использовать fallback-загрузку UEFI и загрузить Linux с флеш-диска:</p>

<ul>
  <li>
    <p>отформатируем флеш-диск файловой системой <em>FAT32</em>,</p>
  </li>
  <li>
    <p>переименуем файл образа ядра Linux в <code class="language-plaintext highlighter-rouge">BOOTX64.EFI</code>,</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">BOOTX64.EFI</code> поместим в директорию <code class="language-plaintext highlighter-rouge">/EFI/BOOT</code>.</p>
  </li>
</ul>

<h4 id="загрузчик">Загрузчик</h4>

<p><em>Загрузчик</em> играет важную роль в процессе запуска Linux. Его можно представить как некоторую программу, которая способна запустить ядро Linux. Ядру, как и обычной программе, запущенной из командной строки, можно передавать <em>параметры командной строки</em>. Требования к тому, как загрузчик передаёт параметры и управление ядру называются <em>протокол загрузки (Boot Protocol)</em>. Протоколы загрузки отличаются для разных платформ, некоторые платформы поддерживают несколько протоколов загрузки.</p>

<p>Мы будем использовать протокол загрузки EFI. Суть его заключается в следующем:</p>

<p>Для загрузки используются возможности UEFI. В этом случае файл ядра является валидным UEFI-приложением. В самом файле ядра есть небольшой код, который подготавливает ядро к вызову функции <code class="language-plaintext highlighter-rouge">start_kernel</code>. Этот код называется <em>EFIStub</em>.</p>

<p>UEFI поддерживает так называемые <em>Boot Entries</em> — записи в <em>NVRAM</em> о том, какие ядра доступны, на каких разделах они располагаются, какую командную строку им нужно передавать. Но эту возможность при использовании сменных носителей не используют.</p>

<p>Также существует возможность поместить <em>Linux Command Line</em> в файл образа ядра. В целях упрощения мы также использовать не будем eё использовать.</p>

<h4 id="командная-строка-ядра-linux">Командная строка ядра Linux</h4>

<p>Хотя в полученном Linux мы не будем использовать командную строку ядра Linux, я приведу о ней краткую информацию. В командной строке передаются такие значения как:</p>

<ul>
  <li>
    <p>кастомный путь и имя файла для процесса init;</p>
  </li>
  <li>
    <p>блочное устройство (раздел на диске), которое содержит корневую файловую систему;</p>
  </li>
  <li>
    <p>какое устройство будет использоваться в качестве консоли при загрузке.</p>
  </li>
</ul>

<p>Кроме того, через командную строку передаются параметры программам, запускающиеся с initramfs. Поэтому от дистрибутива к дистрибутиву список параметров, которые можно передать в качестве параметров ядра, различается.</p>

<p>Какая командная строка была передана ядру, можно посмотреть следующим образом:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ cat /proc/cmdline
</code></pre></div></div>

<h3 id="сборка">Сборка</h3>

<p><em>Сборка операционной системы Linux</em> подразумевает сборку ядра, сборку пользовательских программ, создание и наполнение корневой файловой системы.</p>

<p>Обычно для этой цели используются специализированные средства, но так как статья сфокусирована на простоте, я рассматриваю самый простой случай, когда вы сможете собрать Linux на коленке.</p>

<p>Для сборки нам понадобятся:</p>

<ul>
  <li>
    <p>Docker;</p>
  </li>
  <li>
    <p>Docker-образ Linux-дистрибутива;</p>
  </li>
  <li>
    <p>пакеты, которые содержат программы, позволяющие собрать ядро и пользовательские программы;</p>
  </li>
  <li>
    <p>исходники ванильного ядра Linux c сайта <em>kernel.org</em>;</p>
  </li>
  <li>
    <p>исходники BusyBox;</p>
  </li>
  <li>
    <p>немного терпения и умение работать с командной строкой Linux.</p>
  </li>
</ul>

<p>Кросс-компиляцию, а также создание Linux-системы с initramfs и постоянной корневой файловой системой я рассматривать не буду.</p>

<p>Порядок изложения выбран так, чтобы шаги сборки шли в нужной последовательности — это позволяет удобно выполнять команды по ходу текста. Поэтому структура немного отличается от той, что была бы при чисто теоретическом описании.</p>

<h4 id="создание-окружения-для-сборки">Создание окружения для сборки</h4>

<ol>
  <li>
    <p>Скачать и установить <a href="https://www.docker.com/">Docker</a>, если он у вас не установлен.</p>
  </li>
  <li>
    <p>Запустить Docker-контейнер в окне терминала:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run -it --rm -v "%cd%":/result debian:bookworm bash
</code></pre></div>    </div>
  </li>
  <li>
    <p>Установить пакеты:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># apt update &amp;&amp; apt install -y apt-file &amp;&amp; apt-file update
# apt install -y build-essential wget xz-utils cpio flex bison bc \
file tree qemu-system  ncurses-dev libelf-dev libssl-dev
</code></pre></div>    </div>
  </li>
  <li>
    <p>Создать директорию для сборки</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># mkdir build &amp;&amp; cd build
</code></pre></div>    </div>
  </li>
</ol>

<h4 id="сборка-пользовательских-программ">Сборка пользовательских программ</h4>

<p>В нашей системе будет только одна пользовательская программа — BusyBox. Исходный код BusyBox включает встроенный конфигуратор, по своему принципу похожий на те, что используются в ядре Linux или в Buildroot — среде, предназначенной для сборки кастомных дистрибутивов из исходного кода.</p>

<ol>
  <li>
    <p>Скачиваем исходный код BusyBox:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># wget https://busybox.net/downloads/busybox-1.37.0.tar.bz2
# tar xvf busybox-1.37.0.tar.bz2
# cd busybox-1.37.0
</code></pre></div>    </div>
  </li>
  <li>
    <p>Создаём конфигурацию по умолчанию:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># make defconfig
</code></pre></div>    </div>
  </li>
  <li>
    <p>Выполняем сборку:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># make CONFIG_STATIC=y
</code></pre></div>    </div>
  </li>
  <li>
    <p>Убеждаемся, что BusyBox был собран со статической линковкой:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># file busybox
busybox: ELF 64-bit LSB executable, x86-64, version 1 (GNU/Linux), statically linked, BuildID[sha1]=6540c48477b6eae56a14377ae0de4d0229ea75dc, for GNU/Linux 3.2.0, stripped
</code></pre></div>    </div>
  </li>
</ol>

<h4 id="создание-корневой-файловой-системы">Создание корневой файловой системы</h4>

<p>Наша Linux-система не будет иметь полноценную корневую файловую систему. Вместо этого мы будем использовать только initramfs-образ.</p>

<p>Initramfs — это архив в формате cpio, который содержит каталоги, файлы пользовательских программ, файлы устройств и конфигурационные файлы.</p>

<p>Чтобы сэкономить место, его можно сжать с помощью стандартного архиватора.</p>

<ol>
  <li>
    <p>Создаём структуру корневой файловой системы:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># mkdir -p ../rootfs/{bin,sbin,etc,proc,sys,usr/{bin,sbin},dev,run,tmp,var}
</code></pre></div>    </div>
  </li>
  <li>
    <p>Выполняем установку BusyBox в директорию с корневой файловой системой:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># make CONFIG_STATIC=y CONFIG_PREFIX=../rootfs install
</code></pre></div>    </div>
  </li>
  <li>
    <p>Добавляем скрипт для системы инициализации:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cat &lt;&lt; EOF &gt; ../rootfs/init
#!/bin/sh
mount -t proc none /proc
mount -t sysfs none /sys
mount -t tmpfs tmpfs /run
mount -t tmpfs tmpfs /tmp
mdev -s
if [ -c /dev/fb0 ]; then
  TTY=/dev/tty1
else
  TTY=/dev/ttyS0
fi
echo 0 &gt; /proc/sys/kernel/printk
printf "\033c" &gt; \$TTY
cat &lt;&lt; INNER &gt; \$TTY
                           /)
                  /\___/\ ((
                  \ @_@'/  ))
                  {_:Y:.}_//
================{_}^-'{_}====================
~                                           ~
~    Welcome to ArtyomSoft Minimal Linux    ~
~                                           ~
=============================================

TTY: \$(basename \$TTY)
Time: \$(date)
Kernel version: \$(uname -r)
=============================================
INNER

exec setsid sh -c "exec sh &lt;'\$TTY' &gt;'\$TTY' 2&gt;'\$TTY'"

EOF
</code></pre></div>    </div>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># chmod +x ../rootfs/init
</code></pre></div>    </div>
  </li>
  <li>
    <p>Создаём initramfs-образ с корневой файловой системой:</p>
  </li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># cd ../rootfs
# find . | cpio -o -H newc | gzip &gt; ../initramfs.cpio.gz
</code></pre></div></div>

<h4 id="сборка-ядра">Сборка ядра</h4>

<p>При сборке ядра важно его правильно сконфигурировать. Для самого минимального ядра используется конфигурация <code class="language-plaintext highlighter-rouge">tinyconfig</code>. Если вы выберите эту конфигурацию, ядро запустится, но не увидите работу такого ядра, так как оно практически никак не взаимодействует с пользовательским пространством и устройствами. Поэтому нужно добавить некоторое количество опций.</p>

<p>Несмотря на то, что определение опций — достаточно сложное занятие, я нахожу его увлекательным. Сильно упрощает работу по определению нужных опций использование <code class="language-plaintext highlighter-rouge">make menuconfig</code> или <code class="language-plaintext highlighter-rouge">make nconfig</code>. В первом приложении после запуска необходимо нажать <code class="language-plaintext highlighter-rouge">/</code>, во втором — <code class="language-plaintext highlighter-rouge">F8</code> и в появившееся окно ввести часть названия опции, которую вы хотите добавить. После ввода появится окно найденных параметров с описанием назначения, зависимостями и указанием того, где они располагаются в меню.</p>

<p><img src="%D0%A1%D0%BE%D0%B1%D0%B8%D1%80%D0%B0%D0%B5%D0%BC%20Linux,%20%D0%BA%D0%BE%D1%82%D0%BE%D1%80%D1%8B%D0%B8%CC%86%20%D0%B2%D0%B5%D1%81%D0%B8%D1%82%20%D0%BC%D0%B5%D0%BD%D1%8C%D1%88%D0%B5,%20%D1%87%D0%B5%D0%BC%20%D0%BC%D0%B5%D0%BC%20%D1%81%20%D0%BA%D0%BE%D1%82%D0%B8%D0%BA%D0%BE%D0%BC,%20%D0%BD%D1%83%20%D0%B8%D0%BB%D0%B8%20%D0%BF%D0%BE%D1%87%D1%82%D0%B8%E2%80%A6%20_%20%D0%A5%D0%B0%D0%B1%D1%80_files/iu_xm_onnnhytvbo12isaftobzk.png" alt="Окно поиска опций ядра menuconfig" /></p>

<p>Окно поиска опций ядра menuconfig</p>

<p><img src="%D0%A1%D0%BE%D0%B1%D0%B8%D1%80%D0%B0%D0%B5%D0%BC%20Linux,%20%D0%BA%D0%BE%D1%82%D0%BE%D1%80%D1%8B%D0%B8%CC%86%20%D0%B2%D0%B5%D1%81%D0%B8%D1%82%20%D0%BC%D0%B5%D0%BD%D1%8C%D1%88%D0%B5,%20%D1%87%D0%B5%D0%BC%20%D0%BC%D0%B5%D0%BC%20%D1%81%20%D0%BA%D0%BE%D1%82%D0%B8%D0%BA%D0%BE%D0%BC,%20%D0%BD%D1%83%20%D0%B8%D0%BB%D0%B8%20%D0%BF%D0%BE%D1%87%D1%82%D0%B8%E2%80%A6%20_%20%D0%A5%D0%B0%D0%B1%D1%80_files/qv_lqjz_da1cucp1hijo4mur__u.png" alt="Окно с описанием опций ядра menuconfig" /></p>

<p>Окно с описанием опций ядра menuconfig</p>

<p>Опции можно выбирать непосредственно в интерфейсе приложения, а можно, используя скрипт ‘./scripts/config’. Помните, что если вы выбрали опцию, зависящую от опции, которая не включена у вас в конфигурации, то опция будет молчаливо проигнорирована при сборке.</p>

<blockquote>
  <p>Если вы добавили какую-то опцию и попытаетесь собрать ядро, часто сборщик ядра у вас запросит дополнительные опции для конфигурирования. Можно просто множество раз нажимать клавишу <code class="language-plaintext highlighter-rouge">Enter</code>, а можно просто перед запуском сборки ввести команду:</p>

  <p><code class="language-plaintext highlighter-rouge">$ make olddefconfig</code></p>

  <p>и недоконфигурированные опции примут значения по умолчанию.</p>
</blockquote>

<p>Ниже я привожу, какие параметры я добавил и зачем.</p>

<table>
  <thead>
    <tr>
      <th>Параметр</th>
      <th>Назначение</th>
      <th>Комментарий / зачем включать</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>64BIT</strong></td>
      <td>Поддержка 64-битной архитектуры (x86_64).</td>
      <td>Необходимо для сборки 64-битного ядра.</td>
    </tr>
    <tr>
      <td><strong>PCI</strong></td>
      <td>Поддержка шины PCI.</td>
      <td>Позволяет обнаруживать и использовать устройства PCI.</td>
    </tr>
    <tr>
      <td><strong>PCI_HOST_GENERIC</strong></td>
      <td>Универсальный драйвер PCI-хоста.</td>
      <td>Минимальный вариант для QEMU и виртуальных машин.</td>
    </tr>
    <tr>
      <td><strong>TTY</strong></td>
      <td>Поддержка терминалов (tty).</td>
      <td>Без этого не будет консоли и ввода/вывода.</td>
    </tr>
    <tr>
      <td><strong>BLK_DEV_INITRD</strong></td>
      <td>Поддержка initrd/initramfs как блочного устройства.</td>
      <td>Требуется для загрузки с initramfs.</td>
    </tr>
    <tr>
      <td><strong>SERIAL_8250</strong></td>
      <td>Драйвер UART 8250/16550.</td>
      <td>Нужен для вывода через COM-порт.</td>
    </tr>
    <tr>
      <td><strong>SERIAL_8250_CONSOLE</strong></td>
      <td>Использование последовательного порта как консоли.</td>
      <td>Позволяет видеть вывод ядра через UART.</td>
    </tr>
    <tr>
      <td><strong>BINFMT_ELF</strong></td>
      <td>Поддержка ELF-бинарников.</td>
      <td>Формат исполняемых файлов Linux.</td>
    </tr>
    <tr>
      <td><strong>BINFMT_MISC</strong></td>
      <td>Поддержка нестандартных бинарных форматов.</td>
      <td>Например, для интерпретаторов (Python, Wine и др.).</td>
    </tr>
    <tr>
      <td><strong>BINFMT_SCRIPT</strong></td>
      <td>Запуск скриптов с <code class="language-plaintext highlighter-rouge">#!</code>.</td>
      <td>Нужен для shell-скриптов и init.</td>
    </tr>
    <tr>
      <td><strong>DEVTMPFS</strong></td>
      <td>Автоматическое создание <code class="language-plaintext highlighter-rouge">/dev</code>.</td>
      <td>Cлужит для размещения файлов устройств.</td>
    </tr>
    <tr>
      <td><strong>DEVTMPFS_MOUNT</strong></td>
      <td>Автоматическое монтирование <code class="language-plaintext highlighter-rouge">/dev</code>.</td>
      <td>Упрощает запуск init.</td>
    </tr>
    <tr>
      <td><strong>TMPFS</strong></td>
      <td>Временная файловая система в RAM.</td>
      <td>Используется для <code class="language-plaintext highlighter-rouge">/tmp</code>, <code class="language-plaintext highlighter-rouge">/run</code>.</td>
    </tr>
    <tr>
      <td><strong>PRINTK</strong></td>
      <td>Вывод диагностических сообщений ядра.</td>
      <td>Нужен для логов и отладки.</td>
    </tr>
    <tr>
      <td><strong>EARLY_PRINTK</strong></td>
      <td>Ранний вывод сообщений ядра.</td>
      <td>Показывает ошибки до инициализации консоли.</td>
    </tr>
    <tr>
      <td><strong>PRINTK_TIME</strong></td>
      <td>Метки времени в <code class="language-plaintext highlighter-rouge">printk</code>.</td>
      <td>Удобно для отладки и анализа.</td>
    </tr>
    <tr>
      <td><strong>PROC_FS</strong></td>
      <td>Поддержка <code class="language-plaintext highlighter-rouge">/proc</code>.</td>
      <td>Требуется для многих утилит (<code class="language-plaintext highlighter-rouge">ps</code>, <code class="language-plaintext highlighter-rouge">top</code>).</td>
    </tr>
    <tr>
      <td><strong>SYSFS</strong></td>
      <td>Поддержка <code class="language-plaintext highlighter-rouge">/sys</code>.</td>
      <td>Используется системными инструментами.</td>
    </tr>
    <tr>
      <td><strong>INITRAMFS_SOURCE</strong></td>
      <td>Путь к initramfs (например <code class="language-plaintext highlighter-rouge">"../initramfs.cpio.gz"</code>).</td>
      <td>Позволяет встроить initramfs в ядро.</td>
    </tr>
    <tr>
      <td><strong>RD_GZIP</strong></td>
      <td>Поддержка gzip-сжатых initrd/initramfs.</td>
      <td>Нужна, если initramfs сжат gzip-ом.</td>
    </tr>
    <tr>
      <td><strong>INITRAMFS_COMPRESSION_GZIP</strong></td>
      <td>Сжатие встроенного initramfs при сборке.</td>
      <td>Экономит место.</td>
    </tr>
    <tr>
      <td><strong>ACPI</strong></td>
      <td>Поддержка ACPI (управление питанием и устройствами).</td>
      <td>Важно для ПК и ноутбуков, не нужно в простых VM.</td>
    </tr>
    <tr>
      <td><strong>EFI</strong></td>
      <td>Поддержка загрузки через UEFI.</td>
      <td>Обязательно, если система использует UEFI.</td>
    </tr>
    <tr>
      <td><strong>EFI_STUB</strong></td>
      <td>Позволяет ядру быть EFI-приложением.</td>
      <td>Для прямой загрузки ядра без GRUB.</td>
    </tr>
    <tr>
      <td><strong>FB</strong></td>
      <td>Поддержка framebuffer-графики.</td>
      <td>Для вывода на экран без X11.</td>
    </tr>
    <tr>
      <td><strong>FB_EFI</strong></td>
      <td>Использование EFI framebuffer.</td>
      <td>Для вывода на экран в UEFI.</td>
    </tr>
    <tr>
      <td><strong>FRAMEBUFFER_CONSOLE</strong></td>
      <td>Консоль на framebuffer.</td>
      <td>Позволяет видеть текст на графическом экране.</td>
    </tr>
  </tbody>
</table>

<ol>
  <li>
    <p>Скачиваем исходный код ядра Linux:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># cd ..
# wget https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.12.56.tar.xz
# tar xvf linux-6.12.56.tar.xz
# cd linux-6.12.56
</code></pre></div>    </div>
  </li>
  <li>
    <p>Создаём минимально возможную конфигурацию tinyconfig:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># make tinyconfig
</code></pre></div>    </div>
  </li>
  <li>
    <p>Устанавливаем нужные опции для компиляции ядра:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># ./scripts/config \
-e 64BIT \
-e PCI \
-e PCI_HOST_GENERIC \
-e TTY \
-e BLK_DEV_INITRD \
-e SERIAL_8250 \
-e SERIAL_8250_CONSOLE \
-e BINFMT_ELF \
-e BINFMT_MISC \
-e BINFMT_SCRIPT \
-e DEVTMPFS \
-e DEVTMPFS_MOUNT \
-e TMPFS \
-e PRINTK \
-e EARLY_PRINTK \
-e PRINTK_TIME\
-e PROC_FS \
-e SYSFS \
--set-str INITRAMFS_SOURCE "../initramfs.cpio.gz" \
-e RD_GZIP \
-e INITRAMFS_COMPRESSION_GZIP \
-e ACPI \
-e EFI \
-e EFI_STUB \
-e FB \
-e FB_EFI \
-e FRAMEBUFFER_CONSOLE
</code></pre></div>    </div>
  </li>
  <li>
    <p>Собираем ядро:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># make olddefconfig
# make -j$(nproc)
</code></pre></div>    </div>
  </li>
  <li>
    <p>Копируем файл ядра в директорию:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># mkdir -p /result/ESP/EFI/BOOT
# cp arch/x86/boot/bzImage /result/ESP/EFI/BOOT/BOOTX64.EFI
</code></pre></div>    </div>
  </li>
  <li>
    <p>Добавляем EFI Firmware для QEMU</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># cp /usr/share/OVMF/OVMF_VARS.fd /result/
# cp /usr/share/OVMF/OVMF_CODE.fd /result/
</code></pre></div>    </div>
  </li>
  <li>
    <p>Запускаем наш Linux в QEMU:</p>

    <p>В Docker:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># qemu-system-x86_64 -m 4096m -kernel /result/ESP/EFI/BOOT/BOOTX64.EFI -append "console=ttyS0" -nographic
</code></pre></div>    </div>

    <p>В Windows, чтобы проверить, как всё будет выглядеть при запуске на реальном железе:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qemu-system-x86_64 -m 4096m -drive if=pflash,format=raw,readonly=on,file=./OVMF_CODE.fd -drive if=pflash,format=raw,readonly=on,file=./OVMF_VARS.fd -drive format=raw,file=fat:rw:./esp
</code></pre></div>    </div>
  </li>
  <li>
    <p>Для выхода из QEMU нажимае <code class="language-plaintext highlighter-rouge">Ctlr+A</code> <code class="language-plaintext highlighter-rouge">X</code></p>
  </li>
</ol>

<h4 id="запуск-на-компьютере">Запуск на компьютере</h4>

<p>Проверить работу собранного Linux вы можете не только в QEMU, но и на компьютере.</p>

<ol>
  <li>
    <p>Записать файл с ядром на флеш-диск.</p>
  </li>
  <li>
    <p>Инициировать перезагрузку и зайти в UEFI/BIOS Setup Utility.</p>
  </li>
  <li>
    <p>Убедиться, что у вас в UEFI отключён Secure Boot.</p>
  </li>
  <li>
    <p>Выбрать флеш-диск с файлом ядра в качестве загрузочного устройства.</p>
  </li>
  <li>
    <p>Выполнить загрузку с флеш-диска.</p>
  </li>
  <li>
    <p>Поэкспериметировать с командами Linux.</p>
  </li>
  <li>
    <p>Выключить компьютер.</p>
  </li>
</ol>

<p>Как выглядит работа с собранным Linux на реально железе, показано на видео ниже:</p>

<h3 id="выводы">Выводы</h3>

<p>Мы создали почти минимально возможный Linux для UEFI-систем x86_64.</p>

<p>Размер можно уменьшить ещё немного, если использовать musl в BusyBox и отключить некоторые опции при сборке. Но чтобы не усложнять материал, я этого не делал. Думаю, Linux размером около 2,5 МБ, в котором можно работать, когда современные системы занимают гигабайты, сам по себе впечатляет.</p>

<p>В статье я рассмотрел лишь малую часть того, что обычно присутствует в дистрибутиве, но при этом построил рабочую систему, на которой уже можно изучать базовые команды Linux. Она занимает очень мало места, быстро загружается и не требует установки.</p>

<p>В системе нет:</p>

<ul>
  <li>
    <p>поддержки дисковых файловых систем;</p>
  </li>
  <li>
    <p>полноценной поддержки USB;</p>
  </li>
  <li>
    <p>Ethernet;</p>
  </li>
  <li>
    <p>Wi-Fi;</p>
  </li>
  <li>
    <p>TCP/IP и сетевых интерфейсов;</p>
  </li>
  <li>
    <p>аппаратного ускорения графики;</p>
  </li>
  <li>
    <p>пользовательских программ, кроме одного статически слинкованного BusyBox;</p>
  </li>
  <li>
    <p>разделяемых библиотек;</p>
  </li>
  <li>
    <p>полноценной системы инициализации;</p>
  </li>
  <li>
    <p>полноценной корневой файловой системы;</p>
  </li>
  <li>
    <p>модулей ядра;</p>
  </li>
  <li>
    <p>подсистемы логирования;</p>
  </li>
  <li>
    <p>подсистемы udev;</p>
  </li>
  <li>
    <p>многопользовательского режима;</p>
  </li>
  <li>
    <p>графического интерфейса.</p>
  </li>
</ul>

<p>Тем не менее статья даёт понимание базового фундамента, который делает изучение более сложных аспектов Linux значительно проще.</p>

<p>Статья должна быть полезной, если вы только начинаете изучать Embedded-разработку: вы лучше поймёте, зачем создавали <a href="https://buildroot.org/">buildroot</a> и <a href="https://www.yoctoproject.org/">Yocto</a>.</p>

<p>После прочтения статьи принципы построения Unified Kernel Image (UKI) и основы systemd становятся понятнее, так же как и инструменты, создающие initramfs, — такие как Dracut или mkinitramfs.</p>

<p>Одним из минусов созданного Linux является невозможность сохранить результаты работы: всё хранится в RAM и исчезает после перезагрузки. Но это уже тема для отдельной статьи, если вам будет интересно.</p>]]></content><author><name></name></author><category term="habr" /><summary type="html"><![CDATA[Мне всегда было интересно, насколько минимальным может быть Linux.]]></summary></entry><entry><title type="html">How I Setup Neovim On My Mac To Make it AMAZING in 2024</title><link href="http://localhost:4000/other/2025/05/13/how-to-setup-neovim-2024.html" rel="alternate" type="text/html" title="How I Setup Neovim On My Mac To Make it AMAZING in 2024" /><published>2025-05-13T00:00:00+03:00</published><updated>2025-05-13T00:00:00+03:00</updated><id>http://localhost:4000/other/2025/05/13/how-to-setup-neovim-2024</id><content type="html" xml:base="http://localhost:4000/other/2025/05/13/how-to-setup-neovim-2024.html"><![CDATA[<p>original: <a href="https://www.josean.com/posts/how-to-setup-neovim-2024">https://www.josean.com/posts/how-to-setup-neovim-2024</a></p>

<p>You can find the source code for my config <a href="https://github.com/josean-dev/dev-environment-files">here</a>.</p>

<h2 id="open-a-terminal-window">Open a terminal window</h2>

<p>Open a terminal window on your mac. You will need a true color terminal for the colorscheme to work properly.</p>

<p>I’m using <em>iTerm2</em></p>

<h2 id="install-homebrew">Install Homebrew</h2>

<p>Run the following command:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
</code></pre></div></div>

<p>If necessary, when prompted, enter your password here and press enter. If you haven’t installed the XCode Command Line Tools, when prompted, press enter and homebrew will install this as well.</p>

<h2 id="add-to-path-only-apple-silicon-macbooks">Add To Path (Only Apple Silicon Macbooks)</h2>

<p>After installing, add it to the path. This step shouldn’t be necessary on Intel macs.</p>

<p>Run the following two commands to do so:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' &gt;&gt; ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
</code></pre></div></div>

<h2 id="install-iterm2-if-necessary">Install iTerm2 If Necessary</h2>

<p>If you don’t have a true color terminal, install iTerm2 with homebrew:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew install --cask iterm2
</code></pre></div></div>

<p>Then switch to this terminal.</p>

<h2 id="install-a-nerd-font">Install A Nerd Font</h2>

<p>I use Meslo Nerd Font. To install it do:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap homebrew/cask-fonts
</code></pre></div></div>

<p>And then do:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew install font-meslo-lg-nerd-font
</code></pre></div></div>

<p>Then open iTerm2 settings with <code class="language-plaintext highlighter-rouge">CMD+,</code> and under <strong>Profiles &gt; Text</strong> change the font to <strong>MesloLGS Nerd Font Mono</strong></p>

<h2 id="install-neovim">Install Neovim</h2>

<p>Run:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew install neovim
</code></pre></div></div>

<h2 id="install-ripgrep">Install Ripgrep</h2>

<p>Run:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew install ripgrep
</code></pre></div></div>

<h2 id="install-node">Install Node</h2>

<p>Run:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew install node
</code></pre></div></div>

<h2 id="setup-initial-file-structure">Setup Initial File Structure</h2>

<p>Your config will be located in <code class="language-plaintext highlighter-rouge">~/.config/nvim</code>.</p>

<p>Let’s setup the initial file structure with the following commands:</p>

<p>Make the nvim config directory.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkdir -p ~/.config/nvim
</code></pre></div></div>

<p><em><code class="language-plaintext highlighter-rouge">-p</code> is used to also create parent directories if they don’t already exist</em></p>

<p>Move to this directory:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cd ~/.config/nvim
</code></pre></div></div>

<p>Create main <code class="language-plaintext highlighter-rouge">init.lua</code> file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>touch init.lua
</code></pre></div></div>

<p>Create <code class="language-plaintext highlighter-rouge">lua/josean/core</code> directories:</p>

<p><em>Any time I use “josean” you can replace this with your name</em></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkdir -p lua/josean/core
</code></pre></div></div>

<p>Create plugins directory (will have all of the plugin configs/specs):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkdir -p lua/josean/plugins
</code></pre></div></div>

<p>Create <code class="language-plaintext highlighter-rouge">lazy.lua</code> file (will be used to setup/configure lazy.nvim plugin manager):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>touch lua/josean/lazy.lua
</code></pre></div></div>

<h2 id="setup-core-options">Setup core options</h2>

<p>Make sure you’re in <code class="language-plaintext highlighter-rouge">~/.config/nvim</code> and open the config:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nvim .
</code></pre></div></div>

<p>Navigate to the core folder and press <code class="language-plaintext highlighter-rouge">%</code> to create a file and call it: “options.lua”</p>

<p>In this file add:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vim.cmd("let g:netrw_liststyle = 3")
</code></pre></div></div>

<p>Open the explorer with <code class="language-plaintext highlighter-rouge">:Explore</code> and navigate to the main <code class="language-plaintext highlighter-rouge">init.lua</code> file.</p>

<p>Add the following to load the basic options on startup:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require("josean.core.options")
</code></pre></div></div>

<p>Close Neovim with <code class="language-plaintext highlighter-rouge">:w</code> and reopen it with <code class="language-plaintext highlighter-rouge">nvim .</code></p>

<p>Go back to “options.lua” and add the following to setup the rest of the options:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local opt = vim.opt -- for conciseness

-- line numbers
opt.relativenumber = true -- show relative line numbers
opt.number = true -- shows absolute line number on cursor line (when relative number is on)

-- tabs &amp; indentation
opt.tabstop = 2 -- 2 spaces for tabs (prettier default)
opt.shiftwidth = 2 -- 2 spaces for indent width
opt.expandtab = true -- expand tab to spaces
opt.autoindent = true -- copy indent from current line when starting new one

-- line wrapping
opt.wrap = false -- disable line wrapping

-- search settings
opt.ignorecase = true -- ignore case when searching
opt.smartcase = true -- if you include mixed case in your search, assumes you want case-sensitive

-- cursor line
opt.cursorline = true -- highlight the current cursor line

-- appearance

-- turn on termguicolors for nightfly colorscheme to work
-- (have to use iterm2 or any other true color terminal)
opt.termguicolors = true
opt.background = "dark" -- colorschemes that can be light or dark will be made dark
opt.signcolumn = "yes" -- show sign column so that text doesn't shift

-- backspace
opt.backspace = "indent,eol,start" -- allow backspace on indent, end of line or insert mode start position

-- clipboard
opt.clipboard:append("unnamedplus") -- use system clipboard as default register

-- split windows
opt.splitright = true -- split vertical window to the right
opt.splitbelow = true -- split horizontal window to the bottom

-- turn off swapfile
opt.swapfile = false
</code></pre></div></div>

<p>Do <code class="language-plaintext highlighter-rouge">:e lua/josean/core/init.lua</code></p>

<p>Add the following:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require("josean.core.options")
</code></pre></div></div>

<p>Open the explorer with <code class="language-plaintext highlighter-rouge">:Explore</code> and go to the main init.lua file and change the require to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require("josean.core")
</code></pre></div></div>

<h2 id="setup-core-keymaps">Setup core keymaps</h2>

<p>Do <code class="language-plaintext highlighter-rouge">:e lua/josean/core/keymaps.lua</code></p>

<p>And add the following to this file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>-- set leader key to space
vim.g.mapleader = " "

local keymap = vim.keymap -- for conciseness

---------------------
-- General Keymaps -------------------

-- use jk to exit insert mode
keymap.set("i", "jk", "&lt;ESC&gt;", { desc = "Exit insert mode with jk" })

-- clear search highlights
keymap.set("n", "&lt;leader&gt;nh", ":nohl&lt;CR&gt;", { desc = "Clear search highlights" })

-- delete single character without copying into register
-- keymap.set("n", "x", '"_x')

-- increment/decrement numbers
keymap.set("n", "&lt;leader&gt;+", "&lt;C-a&gt;", { desc = "Increment number" }) -- increment
keymap.set("n", "&lt;leader&gt;-", "&lt;C-x&gt;", { desc = "Decrement number" }) -- decrement

-- window management
keymap.set("n", "&lt;leader&gt;sv", "&lt;C-w&gt;v", { desc = "Split window vertically" }) -- split window vertically
keymap.set("n", "&lt;leader&gt;sh", "&lt;C-w&gt;s", { desc = "Split window horizontally" }) -- split window horizontally
keymap.set("n", "&lt;leader&gt;se", "&lt;C-w&gt;=", { desc = "Make splits equal size" }) -- make split windows equal width &amp; height
keymap.set("n", "&lt;leader&gt;sx", "&lt;cmd&gt;close&lt;CR&gt;", { desc = "Close current split" }) -- close current split window

keymap.set("n", "&lt;leader&gt;to", "&lt;cmd&gt;tabnew&lt;CR&gt;", { desc = "Open new tab" }) -- open new tab
keymap.set("n", "&lt;leader&gt;tx", "&lt;cmd&gt;tabclose&lt;CR&gt;", { desc = "Close current tab" }) -- close current tab
keymap.set("n", "&lt;leader&gt;tn", "&lt;cmd&gt;tabn&lt;CR&gt;", { desc = "Go to next tab" }) --  go to next tab
keymap.set("n", "&lt;leader&gt;tp", "&lt;cmd&gt;tabp&lt;CR&gt;", { desc = "Go to previous tab" }) --  go to previous tab
keymap.set("n", "&lt;leader&gt;tf", "&lt;cmd&gt;tabnew %&lt;CR&gt;", { desc = "Open current buffer in new tab" }) --  move current buffer to new tab
</code></pre></div></div>

<p>Open the explorer with <code class="language-plaintext highlighter-rouge">:Explore</code>, open <code class="language-plaintext highlighter-rouge">lua/josean/core/init.lua</code> and add the following:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require("josean.core.options")
require("josean.core.keymaps")
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim .</code></p>

<h2 id="setup-lazynvim">Setup lazy.nvim</h2>

<p>Go to “lazy.lua” and add the following to bootstrap lazy.nvim</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not vim.loop.fs_stat(lazypath) then
  vim.fn.system({
    "git",
    "clone",
    "--filter=blob:none",
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable", -- latest stable release
    lazypath,
  })
end
vim.opt.rtp:prepend(lazypath)
</code></pre></div></div>

<p>Then configure lazy.nvim with the following:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not vim.loop.fs_stat(lazypath) then
  vim.fn.system({
    "git",
    "clone",
    "--filter=blob:none",
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable", -- latest stable release
    lazypath,
  })
end
vim.opt.rtp:prepend(lazypath)

require("lazy").setup("josean.plugins")
</code></pre></div></div>

<p><em>If you’re using your name instead of “josean”, change that to your name here as well</em></p>

<p>Then open the explorer with <code class="language-plaintext highlighter-rouge">:Explore</code> and navigate to main <code class="language-plaintext highlighter-rouge">init.lua</code> file.</p>

<p>Add the following to it:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>require("josean.core")
require("josean.lazy")
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<p><strong>You can see the lazy.nvim UI now with <code class="language-plaintext highlighter-rouge">:Lazy</code> and you can close the UI with <code class="language-plaintext highlighter-rouge">q</code></strong></p>

<h2 id="install-plenary--vim-tmux-navigator">Install plenary &amp; vim-tmux-navigator</h2>

<p>Do <code class="language-plaintext highlighter-rouge">:e lua/josean/plugins/init.lua</code></p>

<p>Add the following to install <strong>plenary</strong> and <strong>vim-tmux-navigator</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "nvim-lua/plenary.nvim", -- lua functions that many plugins use
  "christoomey/vim-tmux-navigator", -- tmux &amp; split window navigation
}
</code></pre></div></div>

<p>After adding this, save the file and you can install manually by doing <code class="language-plaintext highlighter-rouge">:Lazy</code>, then typing <code class="language-plaintext highlighter-rouge">I</code>.</p>

<p>After install, close the UI with <code class="language-plaintext highlighter-rouge">q</code> and you can manually load a plugin with <code class="language-plaintext highlighter-rouge">:Lazy reload vim-tmux-navigator</code> for example.</p>

<p>Otherwise, you can also exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim .</code> and it’ll happen automatically.</p>

<h2 id="install--configure-tokyonight-colorscheme">Install &amp; configure tokyonight colorscheme</h2>

<p>Do <code class="language-plaintext highlighter-rouge">:e lua/josean/plugins/colorscheme.lua</code></p>

<p>In this file add the following:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  {
    "folke/tokyonight.nvim",
    priority = 1000, -- make sure to load this before all the other start plugins
    config = function()
      local bg = "#011628"
      local bg_dark = "#011423"
      local bg_highlight = "#143652"
      local bg_search = "#0A64AC"
      local bg_visual = "#275378"
      local fg = "#CBE0F0"
      local fg_dark = "#B4D0E9"
      local fg_gutter = "#627E97"
      local border = "#547998"

      require("tokyonight").setup({
        style = "night",
        on_colors = function(colors)
          colors.bg = bg
          colors.bg_dark = bg_dark
          colors.bg_float = bg_dark
          colors.bg_highlight = bg_highlight
          colors.bg_popup = bg_dark
          colors.bg_search = bg_search
          colors.bg_sidebar = bg_dark
          colors.bg_statusline = bg_dark
          colors.bg_visual = bg_visual
          colors.border = border
          colors.fg = fg
          colors.fg_dark = fg_dark
          colors.fg_float = fg
          colors.fg_gutter = fg_gutter
          colors.fg_sidebar = fg_dark
        end,
      })
      -- load the colorscheme here
      vim.cmd([[colorscheme tokyonight]])
    end,
  },
}
</code></pre></div></div>

<p>This will setup <strong>tokyonight</strong> as the colorscheme and modify some of its colors according to my preference.</p>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim .</code></p>

<h2 id="setup-nvim-tree-file-explorer">Setup nvim-tree file explorer</h2>

<p>Do <code class="language-plaintext highlighter-rouge">:e lua/josean/plugins/nvim-tree.lua</code></p>

<p>Add the following to this file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "nvim-tree/nvim-tree.lua",
  dependencies = "nvim-tree/nvim-web-devicons",
  config = function()
    local nvimtree = require("nvim-tree")

    -- recommended settings from nvim-tree documentation
    vim.g.loaded_netrw = 1
    vim.g.loaded_netrwPlugin = 1

    nvimtree.setup({
      view = {
        width = 35,
        relativenumber = true,
      },
      -- change folder arrow icons
      renderer = {
        indent_markers = {
          enable = true,
        },
        icons = {
          glyphs = {
            folder = {
              arrow_closed = "", -- arrow when folder is closed
              arrow_open = "", -- arrow when folder is open
            },
          },
        },
      },
      -- disable window_picker for
      -- explorer to work well with
      -- window splits
      actions = {
        open_file = {
          window_picker = {
            enable = false,
          },
        },
      },
      filters = {
        custom = { ".DS_Store" },
      },
      git = {
        ignore = false,
      },
    })

    -- set keymaps
    local keymap = vim.keymap -- for conciseness

    keymap.set("n", "&lt;leader&gt;ee", "&lt;cmd&gt;NvimTreeToggle&lt;CR&gt;", { desc = "Toggle file explorer" }) -- toggle file explorer
    keymap.set("n", "&lt;leader&gt;ef", "&lt;cmd&gt;NvimTreeFindFileToggle&lt;CR&gt;", { desc = "Toggle file explorer on current file" }) -- toggle file explorer on current file
    keymap.set("n", "&lt;leader&gt;ec", "&lt;cmd&gt;NvimTreeCollapse&lt;CR&gt;", { desc = "Collapse file explorer" }) -- collapse file explorer
    keymap.set("n", "&lt;leader&gt;er", "&lt;cmd&gt;NvimTreeRefresh&lt;CR&gt;", { desc = "Refresh file explorer" }) -- refresh file explorer
  end
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-which-key">Setup which-key</h2>

<p>Which-key is great for seeing what keymaps you can use.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">which-key.lua</code></p>

<p>Add this to the file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "folke/which-key.nvim",
  event = "VeryLazy",
  init = function()
    vim.o.timeout = true
    vim.o.timeoutlen = 500
  end,
  opts = {
    -- your configuration comes here
    -- or leave it empty to use the default settings
    -- refer to the configuration section below
  },
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-telescope-fuzzy-finder">Setup telescope fuzzy finder</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">telescope.lua</code></p>

<p>Add this to the file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "nvim-telescope/telescope.nvim",
  branch = "0.1.x",
  dependencies = {
    "nvim-lua/plenary.nvim",
    { "nvim-telescope/telescope-fzf-native.nvim", build = "make" },
    "nvim-tree/nvim-web-devicons",
  },
  config = function()
    local telescope = require("telescope")
    local actions = require("telescope.actions")

    telescope.setup({
      defaults = {
        path_display = { "smart" },
        mappings = {
          i = {
            ["&lt;C-k&gt;"] = actions.move_selection_previous, -- move to prev result
            ["&lt;C-j&gt;"] = actions.move_selection_next, -- move to next result
            ["&lt;C-q&gt;"] = actions.send_selected_to_qflist + actions.open_qflist,
          },
        },
      },
    })

    telescope.load_extension("fzf")

    -- set keymaps
    local keymap = vim.keymap -- for conciseness

    keymap.set("n", "&lt;leader&gt;ff", "&lt;cmd&gt;Telescope find_files&lt;cr&gt;", { desc = "Fuzzy find files in cwd" })
    keymap.set("n", "&lt;leader&gt;fr", "&lt;cmd&gt;Telescope oldfiles&lt;cr&gt;", { desc = "Fuzzy find recent files" })
    keymap.set("n", "&lt;leader&gt;fs", "&lt;cmd&gt;Telescope live_grep&lt;cr&gt;", { desc = "Find string in cwd" })
    keymap.set("n", "&lt;leader&gt;fc", "&lt;cmd&gt;Telescope grep_string&lt;cr&gt;", { desc = "Find string under cursor in cwd" })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-a-greeter">Setup a greeter</h2>

<p>We’re gonna setup a greeter for Neovim startup with alpha-nvim</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">alpha.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "goolord/alpha-nvim",
  event = "VimEnter",
  config = function()
    local alpha = require("alpha")
    local dashboard = require("alpha.themes.dashboard")

    -- Set header
    dashboard.section.header.val = {
      "                                                     ",
      "  ███╗   ██╗███████╗ ██████╗ ██╗   ██╗██╗███╗   ███╗ ",
      "  ████╗  ██║██╔════╝██╔═══██╗██║   ██║██║████╗ ████║ ",
      "  ██╔██╗ ██║█████╗  ██║   ██║██║   ██║██║██╔████╔██║ ",
      "  ██║╚██╗██║██╔══╝  ██║   ██║╚██╗ ██╔╝██║██║╚██╔╝██║ ",
      "  ██║ ╚████║███████╗╚██████╔╝ ╚████╔╝ ██║██║ ╚═╝ ██║ ",
      "  ╚═╝  ╚═══╝╚══════╝ ╚═════╝   ╚═══╝  ╚═╝╚═╝     ╚═╝ ",
      "                                                     ",
    }

    -- Set menu
    dashboard.section.buttons.val = {
      dashboard.button("e", "  &gt; New File", "&lt;cmd&gt;ene&lt;CR&gt;"),
      dashboard.button("SPC ee", "  &gt; Toggle file explorer", "&lt;cmd&gt;NvimTreeToggle&lt;CR&gt;"),
      dashboard.button("SPC ff", "󰱼 &gt; Find File", "&lt;cmd&gt;Telescope find_files&lt;CR&gt;"),
      dashboard.button("SPC fs", "  &gt; Find Word", "&lt;cmd&gt;Telescope live_grep&lt;CR&gt;"),
      dashboard.button("SPC wr", "󰁯  &gt; Restore Session For Current Directory", "&lt;cmd&gt;SessionRestore&lt;CR&gt;"),
      dashboard.button("q", " &gt; Quit NVIM", "&lt;cmd&gt;qa&lt;CR&gt;"),
    }

    -- Send config to alpha
    alpha.setup(dashboard.opts)

    -- Disable folding on alpha buffer
    vim.cmd([[autocmd FileType alpha setlocal nofoldenable]])
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-automated-session-manager">Setup automated session manager</h2>

<p>Automatic session management is great for auto saving sessions before exiting Neovim and getting back to work when you come back.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">auto-session.lua</code></p>

<p>Add the following to this file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "rmagatti/auto-session",
  config = function()
    local auto_session = require("auto-session")

    auto_session.setup({
      auto_restore_enabled = false,
      auto_session_suppress_dirs = { "~/", "~/Dev/", "~/Downloads", "~/Documents", "~/Desktop/" },
    })

    local keymap = vim.keymap

    keymap.set("n", "&lt;leader&gt;wr", "&lt;cmd&gt;SessionRestore&lt;CR&gt;", { desc = "Restore session for cwd" }) -- restore last workspace session for current directory
    keymap.set("n", "&lt;leader&gt;ws", "&lt;cmd&gt;SessionSave&lt;CR&gt;", { desc = "Save session for auto session root dir" }) -- save workspace session for current working directory
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim .</code></p>

<p>When working in a project, you can now close everything with <code class="language-plaintext highlighter-rouge">:qa</code> and when you open Neovim again in this directory you can use <code class="language-plaintext highlighter-rouge">&lt;leader&gt;wr</code> to restore your workspace/session.</p>

<h2 id="disable-lazynvim-change_detection-notification">Disable lazy.nvim change_detection notification</h2>

<p>Let’s disable the lazy.nvim change_detection notification which I find a bit annoying.</p>

<p>Navigate to <code class="language-plaintext highlighter-rouge">lazy.lua</code> and modify the code like so:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not vim.loop.fs_stat(lazypath) then
  vim.fn.system({
    "git",
    "clone",
    "--filter=blob:none",
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable", -- latest stable release
    lazypath,
  })
end
vim.opt.rtp:prepend(lazypath)

require("lazy").setup("josean.plugins", {
  change_detection = {
    notify = false,
  },
})
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-bufferline-for-better-looking-tabs">Setup bufferline for better looking tabs</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">bufferline.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "akinsho/bufferline.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  version = "*",
  opts = {
    options = {
      mode = "tabs",
      separator_style = "slant",
    },
  },
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-lualine-for-a-better-statusline">Setup lualine for a better statusline</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">lualine.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "nvim-lualine/lualine.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  config = function()
    local lualine = require("lualine")
    local lazy_status = require("lazy.status") -- to configure lazy pending updates count

    local colors = {
      blue = "#65D1FF",
      green = "#3EFFDC",
      violet = "#FF61EF",
      yellow = "#FFDA7B",
      red = "#FF4A4A",
      fg = "#c3ccdc",
      bg = "#112638",
      inactive_bg = "#2c3043",
    }

    local my_lualine_theme = {
      normal = {
        a = { bg = colors.blue, fg = colors.bg, gui = "bold" },
        b = { bg = colors.bg, fg = colors.fg },
        c = { bg = colors.bg, fg = colors.fg },
      },
      insert = {
        a = { bg = colors.green, fg = colors.bg, gui = "bold" },
        b = { bg = colors.bg, fg = colors.fg },
        c = { bg = colors.bg, fg = colors.fg },
      },
      visual = {
        a = { bg = colors.violet, fg = colors.bg, gui = "bold" },
        b = { bg = colors.bg, fg = colors.fg },
        c = { bg = colors.bg, fg = colors.fg },
      },
      command = {
        a = { bg = colors.yellow, fg = colors.bg, gui = "bold" },
        b = { bg = colors.bg, fg = colors.fg },
        c = { bg = colors.bg, fg = colors.fg },
      },
      replace = {
        a = { bg = colors.red, fg = colors.bg, gui = "bold" },
        b = { bg = colors.bg, fg = colors.fg },
        c = { bg = colors.bg, fg = colors.fg },
      },
      inactive = {
        a = { bg = colors.inactive_bg, fg = colors.semilightgray, gui = "bold" },
        b = { bg = colors.inactive_bg, fg = colors.semilightgray },
        c = { bg = colors.inactive_bg, fg = colors.semilightgray },
      },
    }

    -- configure lualine with modified theme
    lualine.setup({
      options = {
        theme = my_lualine_theme,
      },
      sections = {
        lualine_x = {
          {
            lazy_status.updates,
            cond = lazy_status.has_updates,
            color = { fg = "#ff9e64" },
          },
          { "encoding" },
          { "fileformat" },
          { "filetype" },
        },
      },
    })
  end,
}
</code></pre></div></div>

<p>So that lualine can show pending plugin updates through lazy.nvim, open “lazy.lua” and modify it like so:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not vim.loop.fs_stat(lazypath) then
  vim.fn.system({
    "git",
    "clone",
    "--filter=blob:none",
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable", -- latest stable release
    lazypath,
  })
end
vim.opt.rtp:prepend(lazypath)

require("lazy").setup("josean.plugins", {
  checker = {
    enabled = true,
    notify = false,
  },
  change_detection = {
    notify = false,
  },
})
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-dressingnvim">Setup dressing.nvim</h2>

<p>Dressing.nvim improves the ui for <code class="language-plaintext highlighter-rouge">vim.ui.select</code> and <code class="language-plaintext highlighter-rouge">vim.ui.input</code></p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">dressing.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "stevearc/dressing.nvim",
  event = "VeryLazy",
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-vim-maximizer">Setup vim-maximizer</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">vim-maximizer.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "szw/vim-maximizer",
  keys = {
    { "&lt;leader&gt;sm", "&lt;cmd&gt;MaximizerToggle&lt;CR&gt;", desc = "Maximize/minimize a split" },
  },
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-treesitter">Setup treesitter</h2>

<p>Treesitter is an awesome Neovim feature that provides better syntax highlighting, indentation, autotagging, incremental selection and many other cool features.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">treesitter.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "nvim-treesitter/nvim-treesitter",
  event = { "BufReadPre", "BufNewFile" },
  build = ":TSUpdate",
  dependencies = {
    "windwp/nvim-ts-autotag",
  },
  config = function()
    -- import nvim-treesitter plugin
    local treesitter = require("nvim-treesitter.configs")

    -- configure treesitter
    treesitter.setup({ -- enable syntax highlighting
      highlight = {
        enable = true,
      },
      -- enable indentation
      indent = { enable = true },
      -- enable autotagging (w/ nvim-ts-autotag plugin)
      autotag = {
        enable = true,
      },
      -- ensure these language parsers are installed
      ensure_installed = {
        "json",
        "javascript",
        "typescript",
        "tsx",
        "yaml",
        "html",
        "css",
        "prisma",
        "markdown",
        "markdown_inline",
        "svelte",
        "graphql",
        "bash",
        "lua",
        "vim",
        "dockerfile",
        "gitignore",
        "query",
        "vimdoc",
        "c",
      },
      incremental_selection = {
        enable = true,
        keymaps = {
          init_selection = "&lt;C-space&gt;",
          node_incremental = "&lt;C-space&gt;",
          scope_incremental = false,
          node_decremental = "&lt;bs&gt;",
        },
      },
    })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-indent-guides">Setup indent guides</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">indent-blankline.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "lukas-reineke/indent-blankline.nvim",
  event = { "BufReadPre", "BufNewFile" },
  main = "ibl",
  opts = {
    indent = { char = "┊" },
  },
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-autocompletion">Setup autocompletion</h2>

<p>We’re going to setup completion with “nvim-cmp” to get suggestions as we type.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">nvim-cmp.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "hrsh7th/nvim-cmp",
  event = "InsertEnter",
  dependencies = {
    "hrsh7th/cmp-buffer", -- source for text in buffer
    "hrsh7th/cmp-path", -- source for file system paths
    {
      "L3MON4D3/LuaSnip",
      -- follow latest release.
      version = "v2.*", -- Replace &lt;CurrentMajor&gt; by the latest released major (first number of latest release)
      -- install jsregexp (optional!).
      build = "make install_jsregexp",
    },
    "saadparwaiz1/cmp_luasnip", -- for autocompletion
    "rafamadriz/friendly-snippets", -- useful snippets
    "onsails/lspkind.nvim", -- vs-code like pictograms
  },
  config = function()
    local cmp = require("cmp")

    local luasnip = require("luasnip")

    local lspkind = require("lspkind")

    -- loads vscode style snippets from installed plugins (e.g. friendly-snippets)
    require("luasnip.loaders.from_vscode").lazy_load()

    cmp.setup({
      completion = {
        completeopt = "menu,menuone,preview,noselect",
      },
      snippet = { -- configure how nvim-cmp interacts with snippet engine
        expand = function(args)
          luasnip.lsp_expand(args.body)
        end,
      },
      mapping = cmp.mapping.preset.insert({
        ["&lt;C-k&gt;"] = cmp.mapping.select_prev_item(), -- previous suggestion
        ["&lt;C-j&gt;"] = cmp.mapping.select_next_item(), -- next suggestion
        ["&lt;C-b&gt;"] = cmp.mapping.scroll_docs(-4),
        ["&lt;C-f&gt;"] = cmp.mapping.scroll_docs(4),
        ["&lt;C-Space&gt;"] = cmp.mapping.complete(), -- show completion suggestions
        ["&lt;C-e&gt;"] = cmp.mapping.abort(), -- close completion window
        ["&lt;CR&gt;"] = cmp.mapping.confirm({ select = false }),
      }),
      -- sources for autocompletion
      sources = cmp.config.sources({
        { name = "luasnip" }, -- snippets
        { name = "buffer" }, -- text within current buffer
        { name = "path" }, -- file system paths
      }),

      -- configure lspkind for vs-code like pictograms in completion menu
      formatting = {
        format = lspkind.cmp_format({
          maxwidth = 50,
          ellipsis_char = "...",
        }),
      },
    })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-auto-closing-pairs">Setup auto closing pairs</h2>

<p>This plugin will help us auto close surrounding characters like parens, brackets, curly braces, quotes, single quotes and tags</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">autopairs.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "windwp/nvim-autopairs",
  event = { "InsertEnter" },
  dependencies = {
    "hrsh7th/nvim-cmp",
  },
  config = function()
    -- import nvim-autopairs
    local autopairs = require("nvim-autopairs")

    -- configure autopairs
    autopairs.setup({
      check_ts = true, -- enable treesitter
      ts_config = {
        lua = { "string" }, -- don't add pairs in lua string treesitter nodes
        javascript = { "template_string" }, -- don't add pairs in javscript template_string treesitter nodes
        java = false, -- don't check treesitter on java
      },
    })

    -- import nvim-autopairs completion functionality
    local cmp_autopairs = require("nvim-autopairs.completion.cmp")

    -- import nvim-cmp plugin (completions plugin)
    local cmp = require("cmp")

    -- make autopairs and completion work together
    cmp.event:on("confirm_done", cmp_autopairs.on_confirm_done())
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-commenting-plugin">Setup commenting plugin</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">comment.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "numToStr/Comment.nvim",
  event = { "BufReadPre", "BufNewFile" },
  dependencies = {
    "JoosepAlviste/nvim-ts-context-commentstring",
  },
  config = function()
    -- import comment plugin safely
    local comment = require("Comment")

    local ts_context_commentstring = require("ts_context_commentstring.integrations.comment_nvim")

    -- enable comment
    comment.setup({
      -- for commenting tsx, jsx, svelte, html files
      pre_hook = ts_context_commentstring.create_pre_hook(),
    })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-todo-comments">Setup todo comments</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">todo-comments.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "folke/todo-comments.nvim",
  event = { "BufReadPre", "BufNewFile" },
  dependencies = { "nvim-lua/plenary.nvim" },
  config = function()
    local todo_comments = require("todo-comments")

    -- set keymaps
    local keymap = vim.keymap -- for conciseness

    keymap.set("n", "]t", function()
      todo_comments.jump_next()
    end, { desc = "Next todo comment" })

    keymap.set("n", "[t", function()
      todo_comments.jump_prev()
    end, { desc = "Previous todo comment" })

    todo_comments.setup()
  end,
}
</code></pre></div></div>

<p>Look for <code class="language-plaintext highlighter-rouge">telescope.lua</code> with telescope with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ff</code></p>

<p>Open this file and add the following to be able to look for todos with telescope:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "nvim-telescope/telescope.nvim",
  branch = "0.1.x",
  dependencies = {
    "nvim-lua/plenary.nvim",
    { "nvim-telescope/telescope-fzf-native.nvim", build = "make" },
    "nvim-tree/nvim-web-devicons",
    "folke/todo-comments.nvim",
  },
  config = function()
    local telescope = require("telescope")
    local actions = require("telescope.actions")

    telescope.setup({
      defaults = {
        path_display = { "smart" },
        mappings = {
          i = {
            ["&lt;C-k&gt;"] = actions.move_selection_previous, -- move to prev result
            ["&lt;C-j&gt;"] = actions.move_selection_next, -- move to next result
            ["&lt;C-q&gt;"] = actions.send_selected_to_qflist + actions.open_qflist,
          },
        },
      },
    })

    telescope.load_extension("fzf")

    -- set keymaps
    local keymap = vim.keymap -- for conciseness

    keymap.set("n", "&lt;leader&gt;ff", "&lt;cmd&gt;Telescope find_files&lt;cr&gt;", { desc = "Fuzzy find files in cwd" })
    keymap.set("n", "&lt;leader&gt;fr", "&lt;cmd&gt;Telescope oldfiles&lt;cr&gt;", { desc = "Fuzzy find recent files" })
    keymap.set("n", "&lt;leader&gt;fs", "&lt;cmd&gt;Telescope live_grep&lt;cr&gt;", { desc = "Find string in cwd" })
    keymap.set("n", "&lt;leader&gt;fc", "&lt;cmd&gt;Telescope grep_string&lt;cr&gt;", { desc = "Find string under cursor in cwd" })
    keymap.set("n", "&lt;leader&gt;ft", "&lt;cmd&gt;TodoTelescope&lt;cr&gt;", { desc = "Find todos" })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-substitution-plugin">Setup substitution plugin</h2>

<p>This plugin allows us to use <code class="language-plaintext highlighter-rouge">s</code> followed by a <code class="language-plaintext highlighter-rouge">motion</code> to substitute text that was previously copied.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">substitute.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "gbprod/substitute.nvim",
  event = { "BufReadPre", "BufNewFile" },
  config = function()
    local substitute = require("substitute")

    substitute.setup()

    -- set keymaps
    local keymap = vim.keymap -- for conciseness

    vim.keymap.set("n", "s", substitute.operator, { desc = "Substitute with motion" })
    vim.keymap.set("n", "ss", substitute.line, { desc = "Substitute line" })
    vim.keymap.set("n", "S", substitute.eol, { desc = "Substitute to end of line" })
    vim.keymap.set("x", "s", substitute.visual, { desc = "Substitute in visual mode" })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-nvim-surround">Setup nvim-surround</h2>

<p>This plugin is great for adding, deleting and modifying surrounding symbols and tags.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">surround.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "kylechui/nvim-surround",
  event = { "BufReadPre", "BufNewFile" },
  version = "*", -- Use for stability; omit to use `main` branch for the latest features
  config = true,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-lsp">Setup LSP</h2>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">lua/josean/plugins</code> add a new directory with <code class="language-plaintext highlighter-rouge">a</code>, calling it <code class="language-plaintext highlighter-rouge">lsp/</code></p>

<p>Navigate to <code class="language-plaintext highlighter-rouge">lazy.lua</code> and modify it so that <code class="language-plaintext highlighter-rouge">lazy.nvim</code> knows about the new <code class="language-plaintext highlighter-rouge">lsp</code> directory like so:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not vim.loop.fs_stat(lazypath) then
  vim.fn.system({
    "git",
    "clone",
    "--filter=blob:none",
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable", -- latest stable release
    lazypath,
  })
end
vim.opt.rtp:prepend(lazypath)

require("lazy").setup({ { import = "josean.plugins" }, { import = "josean.plugins.lsp" } }, {
  checker = {
    enabled = true,
    notify = false,
  },
  change_detection = {
    notify = false,
  },
})
</code></pre></div></div>

<h3 id="setup-masonnvim">Setup mason.nvim</h3>

<p>Mason.nvim is used to install and manage all of the language servers that you need for the languages you work for.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins/lsp</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">mason.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "williamboman/mason.nvim",
  dependencies = {
    "williamboman/mason-lspconfig.nvim",
  },
  config = function()
    -- import mason
    local mason = require("mason")

    -- import mason-lspconfig
    local mason_lspconfig = require("mason-lspconfig")

    -- enable mason and configure icons
    mason.setup({
      ui = {
        icons = {
          package_installed = "✓",
          package_pending = "➜",
          package_uninstalled = "✗",
        },
      },
    })

    mason_lspconfig.setup({
      -- list of servers for mason to install
      ensure_installed = {
        "tsserver",
        "html",
        "cssls",
        "tailwindcss",
        "svelte",
        "lua_ls",
        "graphql",
        "emmet_ls",
        "prismals",
        "pyright",
      },
    })
  end,
}
</code></pre></div></div>

<h3 id="setup-nvim-lspconfig">Setup nvim-lspconfig</h3>

<p>Nvim-lspconfig is used to configure your language servers.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins/lsp</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">lspconfig.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "neovim/nvim-lspconfig",
  event = { "BufReadPre", "BufNewFile" },
  dependencies = {
    "hrsh7th/cmp-nvim-lsp",
    { "antosha417/nvim-lsp-file-operations", config = true },
    { "folke/neodev.nvim", opts = {} },
  },
  config = function()
    -- import lspconfig plugin
    local lspconfig = require("lspconfig")

    -- import mason_lspconfig plugin
    local mason_lspconfig = require("mason-lspconfig")

    -- import cmp-nvim-lsp plugin
    local cmp_nvim_lsp = require("cmp_nvim_lsp")

    local keymap = vim.keymap -- for conciseness

    vim.api.nvim_create_autocmd("LspAttach", {
      group = vim.api.nvim_create_augroup("UserLspConfig", {}),
      callback = function(ev)
        -- Buffer local mappings.
        -- See `:help vim.lsp.*` for documentation on any of the below functions
        local opts = { buffer = ev.buf, silent = true }

        -- set keybinds
        opts.desc = "Show LSP references"
        keymap.set("n", "gR", "&lt;cmd&gt;Telescope lsp_references&lt;CR&gt;", opts) -- show definition, references

        opts.desc = "Go to declaration"
        keymap.set("n", "gD", vim.lsp.buf.declaration, opts) -- go to declaration

        opts.desc = "Show LSP definitions"
        keymap.set("n", "gd", "&lt;cmd&gt;Telescope lsp_definitions&lt;CR&gt;", opts) -- show lsp definitions

        opts.desc = "Show LSP implementations"
        keymap.set("n", "gi", "&lt;cmd&gt;Telescope lsp_implementations&lt;CR&gt;", opts) -- show lsp implementations

        opts.desc = "Show LSP type definitions"
        keymap.set("n", "gt", "&lt;cmd&gt;Telescope lsp_type_definitions&lt;CR&gt;", opts) -- show lsp type definitions

        opts.desc = "See available code actions"
        keymap.set({ "n", "v" }, "&lt;leader&gt;ca", vim.lsp.buf.code_action, opts) -- see available code actions, in visual mode will apply to selection

        opts.desc = "Smart rename"
        keymap.set("n", "&lt;leader&gt;rn", vim.lsp.buf.rename, opts) -- smart rename

        opts.desc = "Show buffer diagnostics"
        keymap.set("n", "&lt;leader&gt;D", "&lt;cmd&gt;Telescope diagnostics bufnr=0&lt;CR&gt;", opts) -- show  diagnostics for file

        opts.desc = "Show line diagnostics"
        keymap.set("n", "&lt;leader&gt;d", vim.diagnostic.open_float, opts) -- show diagnostics for line

        opts.desc = "Go to previous diagnostic"
        keymap.set("n", "[d", vim.diagnostic.goto_prev, opts) -- jump to previous diagnostic in buffer

        opts.desc = "Go to next diagnostic"
        keymap.set("n", "]d", vim.diagnostic.goto_next, opts) -- jump to next diagnostic in buffer

        opts.desc = "Show documentation for what is under cursor"
        keymap.set("n", "K", vim.lsp.buf.hover, opts) -- show documentation for what is under cursor

        opts.desc = "Restart LSP"
        keymap.set("n", "&lt;leader&gt;rs", ":LspRestart&lt;CR&gt;", opts) -- mapping to restart lsp if necessary
      end,
    })

    -- used to enable autocompletion (assign to every lsp server config)
    local capabilities = cmp_nvim_lsp.default_capabilities()

    -- Change the Diagnostic symbols in the sign column (gutter)
    -- (not in youtube nvim video)
    local signs = { Error = " ", Warn = " ", Hint = "󰠠 ", Info = " " }
    for type, icon in pairs(signs) do
      local hl = "DiagnosticSign" .. type
      vim.fn.sign_define(hl, { text = icon, texthl = hl, numhl = "" })
    end

    mason_lspconfig.setup_handlers({
      -- default handler for installed servers
      function(server_name)
        lspconfig[server_name].setup({
          capabilities = capabilities,
        })
      end,
      ["svelte"] = function()
        -- configure svelte server
        lspconfig["svelte"].setup({
          capabilities = capabilities,
          on_attach = function(client, bufnr)
            vim.api.nvim_create_autocmd("BufWritePost", {
              pattern = { "*.js", "*.ts" },
              callback = function(ctx)
                -- Here use ctx.match instead of ctx.file
                client.notify("$/onDidChangeTsOrJsFile", { uri = ctx.match })
              end,
            })
          end,
        })
      end,
      ["graphql"] = function()
        -- configure graphql language server
        lspconfig["graphql"].setup({
          capabilities = capabilities,
          filetypes = { "graphql", "gql", "svelte", "typescriptreact", "javascriptreact" },
        })
      end,
      ["emmet_ls"] = function()
        -- configure emmet language server
        lspconfig["emmet_ls"].setup({
          capabilities = capabilities,
          filetypes = { "html", "typescriptreact", "javascriptreact", "css", "sass", "scss", "less", "svelte" },
        })
      end,
      ["lua_ls"] = function()
        -- configure lua server (with special settings)
        lspconfig["lua_ls"].setup({
          capabilities = capabilities,
          settings = {
            Lua = {
              -- make the language server recognize "vim" global
              diagnostics = {
                globals = { "vim" },
              },
              completion = {
                callSnippet = "Replace",
              },
            },
          },
        })
      end,
    })
  end,
}
</code></pre></div></div>

<p><em>In the code under <code class="language-plaintext highlighter-rouge">mason_lspconfig.setup_handlers</code> I setup a default for my language servers and some custom configurations for <code class="language-plaintext highlighter-rouge">svelte</code>, <code class="language-plaintext highlighter-rouge">graphql</code>, <code class="language-plaintext highlighter-rouge">emmet_ls</code>, and <code class="language-plaintext highlighter-rouge">lua_ls</code>. This can vary depending on the languages that you’re gonna be using.</em></p>

<p>Navigate to <code class="language-plaintext highlighter-rouge">nvim-cmp.lua</code> and make the following change to add the lsp as a completion source:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "hrsh7th/nvim-cmp",
  event = "InsertEnter",
  dependencies = {
    "hrsh7th/cmp-buffer", -- source for text in buffer
    "hrsh7th/cmp-path", -- source for file system paths
    {
      "L3MON4D3/LuaSnip",
      -- follow latest release.
      version = "v2.*", -- Replace &lt;CurrentMajor&gt; by the latest released major (first number of latest release)
      -- install jsregexp (optional!).
      build = "make install_jsregexp",
    },
    "saadparwaiz1/cmp_luasnip", -- for autocompletion
    "rafamadriz/friendly-snippets", -- useful snippets
    "onsails/lspkind.nvim", -- vs-code like pictograms
  },
  config = function()
    local cmp = require("cmp")

    local luasnip = require("luasnip")

    local lspkind = require("lspkind")

    -- loads vscode style snippets from installed plugins (e.g. friendly-snippets)
    require("luasnip.loaders.from_vscode").lazy_load()

    cmp.setup({
      completion = {
        completeopt = "menu,menuone,preview,noselect",
      },
      snippet = { -- configure how nvim-cmp interacts with snippet engine
        expand = function(args)
          luasnip.lsp_expand(args.body)
        end,
      },
      mapping = cmp.mapping.preset.insert({
        ["&lt;C-k&gt;"] = cmp.mapping.select_prev_item(), -- previous suggestion
        ["&lt;C-j&gt;"] = cmp.mapping.select_next_item(), -- next suggestion
        ["&lt;C-b&gt;"] = cmp.mapping.scroll_docs(-4),
        ["&lt;C-f&gt;"] = cmp.mapping.scroll_docs(4),
        ["&lt;C-Space&gt;"] = cmp.mapping.complete(), -- show completion suggestions
        ["&lt;C-e&gt;"] = cmp.mapping.abort(), -- close completion window
        ["&lt;CR&gt;"] = cmp.mapping.confirm({ select = false }),
      }),
      -- sources for autocompletion
      sources = cmp.config.sources({
        { name = "nvim_lsp"},
        { name = "luasnip" }, -- snippets
        { name = "buffer" }, -- text within current buffer
        { name = "path" }, -- file system paths
      }),

      -- configure lspkind for vs-code like pictograms in completion menu
      formatting = {
        format = lspkind.cmp_format({
          maxwidth = 50,
          ellipsis_char = "...",
        }),
      },
    })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-troublenvim">Setup trouble.nvim</h2>

<p>This is another plugin that adds some nice functionality for interacting with the lsp and some other things like todo comments.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">trouble.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "folke/trouble.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons", "folke/todo-comments.nvim" },
  opts = {
    focus = true,
  },
  cmd = "Trouble",
  keys = {
    { "&lt;leader&gt;xw", "&lt;cmd&gt;Trouble diagnostics toggle&lt;CR&gt;", desc = "Open trouble workspace diagnostics" },
    { "&lt;leader&gt;xd", "&lt;cmd&gt;Trouble diagnostics toggle filter.buf=0&lt;CR&gt;", desc = "Open trouble document diagnostics" },
    { "&lt;leader&gt;xq", "&lt;cmd&gt;Trouble quickfix toggle&lt;CR&gt;", desc = "Open trouble quickfix list" },
    { "&lt;leader&gt;xl", "&lt;cmd&gt;Trouble loclist toggle&lt;CR&gt;", desc = "Open trouble location list" },
    { "&lt;leader&gt;xt", "&lt;cmd&gt;Trouble todo toggle&lt;CR&gt;", desc = "Open todos in trouble" },
  },
}
</code></pre></div></div>

<p><strong>The code above has been refactored to work with trouble version 3. This is different from the code in the video</strong></p>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-formatting">Setup formatting</h2>

<p>We’re gonna use <code class="language-plaintext highlighter-rouge">conform.nvim</code> to setup formatting in Neovim.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">formatting.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "stevearc/conform.nvim",
  event = { "BufReadPre", "BufNewFile" },
  config = function()
    local conform = require("conform")

    conform.setup({
      formatters_by_ft = {
        javascript = { "prettier" },
        typescript = { "prettier" },
        javascriptreact = { "prettier" },
        typescriptreact = { "prettier" },
        svelte = { "prettier" },
        css = { "prettier" },
        html = { "prettier" },
        json = { "prettier" },
        yaml = { "prettier" },
        markdown = { "prettier" },
        graphql = { "prettier" },
        liquid = { "prettier" },
        lua = { "stylua" },
        python = { "isort", "black" },
      },
      format_on_save = {
        lsp_fallback = true,
        async = false,
        timeout_ms = 1000,
      },
    })

    vim.keymap.set({ "n", "v" }, "&lt;leader&gt;mp", function()
      conform.format({
        lsp_fallback = true,
        async = false,
        timeout_ms = 1000,
      })
    end, { desc = "Format file or range (in visual mode)" })
  end,
}
</code></pre></div></div>

<p>Navigate to <code class="language-plaintext highlighter-rouge">mason.lua</code> and add the following to auto install formatters:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "williamboman/mason.nvim",
  dependencies = {
    "williamboman/mason-lspconfig.nvim",
    "WhoIsSethDaniel/mason-tool-installer.nvim",
  },
  config = function()
    -- import mason
    local mason = require("mason")

    -- import mason-lspconfig
    local mason_lspconfig = require("mason-lspconfig")

    local mason_tool_installer = require("mason-tool-installer")

    -- enable mason and configure icons
    mason.setup({
      ui = {
        icons = {
          package_installed = "✓",
          package_pending = "➜",
          package_uninstalled = "✗",
        },
      },
    })

    mason_lspconfig.setup({
      -- list of servers for mason to install
      ensure_installed = {
        "tsserver",
        "html",
        "cssls",
        "tailwindcss",
        "svelte",
        "lua_ls",
        "graphql",
        "emmet_ls",
        "prismals",
        "pyright",
      },
    })

    mason_tool_installer.setup({
      ensure_installed = {
        "prettier", -- prettier formatter
        "stylua", -- lua formatter
        "isort", -- python formatter
        "black", -- python formatter
      },
    })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-linting">Setup linting</h2>

<p>We’re gonna be using nvim-lint to setup linting in Neovim.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">linting.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "mfussenegger/nvim-lint",
  event = { "BufReadPre", "BufNewFile" },
  config = function()
    local lint = require("lint")

    lint.linters_by_ft = {
      javascript = { "eslint_d" },
      typescript = { "eslint_d" },
      javascriptreact = { "eslint_d" },
      typescriptreact = { "eslint_d" },
      svelte = { "eslint_d" },
      python = { "pylint" },
    }

    local lint_augroup = vim.api.nvim_create_augroup("lint", { clear = true })

    vim.api.nvim_create_autocmd({ "BufEnter", "BufWritePost", "InsertLeave" }, {
      group = lint_augroup,
      callback = function()
        lint.try_lint()
      end,
    })

    vim.keymap.set("n", "&lt;leader&gt;l", function()
      lint.try_lint()
    end, { desc = "Trigger linting for current file" })
  end,
}
</code></pre></div></div>

<p>Navigate to <code class="language-plaintext highlighter-rouge">mason.lua</code> and add the following to auto install linters:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "williamboman/mason.nvim",
  dependencies = {
    "williamboman/mason-lspconfig.nvim",
    "WhoIsSethDaniel/mason-tool-installer.nvim",
  },
  config = function()
    -- import mason
    local mason = require("mason")

    -- import mason-lspconfig
    local mason_lspconfig = require("mason-lspconfig")

    local mason_tool_installer = require("mason-tool-installer")

    -- enable mason and configure icons
    mason.setup({
      ui = {
        icons = {
          package_installed = "✓",
          package_pending = "➜",
          package_uninstalled = "✗",
        },
      },
    })

    mason_lspconfig.setup({
      -- list of servers for mason to install
      ensure_installed = {
        "tsserver",
        "html",
        "cssls",
        "tailwindcss",
        "svelte",
        "lua_ls",
        "graphql",
        "emmet_ls",
        "prismals",
        "pyright",
      },
    })

    mason_tool_installer.setup({
      ensure_installed = {
        "prettier", -- prettier formatter
        "stylua", -- lua formatter
        "isort", -- python formatter
        "black", -- python formatter
        "pylint", -- python linter
        "eslint_d", -- js linter
      },
    })
  end,
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="setup-git-functionality">Setup git functionality</h2>

<h3 id="setup-gitsigns-plugin">Setup gitsigns plugin</h3>

<p>Gitsigns is a great plugin for interacting with git hunks in Neovim.</p>

<p>Open the file explorer with <code class="language-plaintext highlighter-rouge">&lt;leader&gt;ee</code> (in my config the <code class="language-plaintext highlighter-rouge">&lt;leader&gt;</code> key is <code class="language-plaintext highlighter-rouge">space</code>).</p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">gitsigns.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "lewis6991/gitsigns.nvim",
  event = { "BufReadPre", "BufNewFile" },
  opts = {
    on_attach = function(bufnr)
      local gs = package.loaded.gitsigns

      local function map(mode, l, r, desc)
        vim.keymap.set(mode, l, r, { buffer = bufnr, desc = desc })
      end

      -- Navigation
      map("n", "]h", gs.next_hunk, "Next Hunk")
      map("n", "[h", gs.prev_hunk, "Prev Hunk")

      -- Actions
      map("n", "&lt;leader&gt;hs", gs.stage_hunk, "Stage hunk")
      map("n", "&lt;leader&gt;hr", gs.reset_hunk, "Reset hunk")
      map("v", "&lt;leader&gt;hs", function()
        gs.stage_hunk({ vim.fn.line("."), vim.fn.line("v") })
      end, "Stage hunk")
      map("v", "&lt;leader&gt;hr", function()
        gs.reset_hunk({ vim.fn.line("."), vim.fn.line("v") })
      end, "Reset hunk")

      map("n", "&lt;leader&gt;hS", gs.stage_buffer, "Stage buffer")
      map("n", "&lt;leader&gt;hR", gs.reset_buffer, "Reset buffer")

      map("n", "&lt;leader&gt;hu", gs.undo_stage_hunk, "Undo stage hunk")

      map("n", "&lt;leader&gt;hp", gs.preview_hunk, "Preview hunk")

      map("n", "&lt;leader&gt;hb", function()
        gs.blame_line({ full = true })
      end, "Blame line")
      map("n", "&lt;leader&gt;hB", gs.toggle_current_line_blame, "Toggle line blame")

      map("n", "&lt;leader&gt;hd", gs.diffthis, "Diff this")
      map("n", "&lt;leader&gt;hD", function()
        gs.diffthis("~")
      end, "Diff this ~")

      -- Text object
      map({ "o", "x" }, "ih", ":&lt;C-U&gt;Gitsigns select_hunk&lt;CR&gt;", "Gitsigns select hunk")
    end,
  },
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code></p>

<h3 id="setup-lazygit-integration">Setup lazygit integration</h3>

<p>Make sure you have lazygit installed.</p>

<p>Install with homebrew:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew install jesseduffield/lazygit/lazygit
</code></pre></div></div>

<p>Open Neovim with <code class="language-plaintext highlighter-rouge">nvim .</code></p>

<p>Under <code class="language-plaintext highlighter-rouge">plugins</code> add a new file with <code class="language-plaintext highlighter-rouge">a</code> and call it <code class="language-plaintext highlighter-rouge">lazygit.lua</code></p>

<p>Add the following code:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return {
  "kdheepak/lazygit.nvim",
  cmd = {
    "LazyGit",
    "LazyGitConfig",
    "LazyGitCurrentFile",
    "LazyGitFilter",
    "LazyGitFilterCurrentFile",
  },
  -- optional for floating window border decoration
  dependencies = {
    "nvim-lua/plenary.nvim",
  },
  -- setting the keybinding for LazyGit with 'keys' is recommended in
  -- order to load the plugin when the command is run for the first time
  keys = {
    { "&lt;leader&gt;lg", "&lt;cmd&gt;LazyGit&lt;cr&gt;", desc = "Open lazy git" },
  },
}
</code></pre></div></div>

<p>Exit with <code class="language-plaintext highlighter-rouge">:q</code> and reenter Neovim with <code class="language-plaintext highlighter-rouge">nvim</code></p>

<h2 id="youre-done-">YOU’RE DONE! 🚀</h2>]]></content><author><name></name></author><category term="other" /><summary type="html"><![CDATA[original: https://www.josean.com/posts/how-to-setup-neovim-2024]]></summary></entry><entry><title type="html">Мой первый контейнер без Docker</title><link href="http://localhost:4000/habr/2025/02/12/docker-no-docker.html" rel="alternate" type="text/html" title="Мой первый контейнер без Docker" /><published>2025-02-12T00:00:00+03:00</published><updated>2025-02-12T00:00:00+03:00</updated><id>http://localhost:4000/habr/2025/02/12/docker-no-docker</id><content type="html" xml:base="http://localhost:4000/habr/2025/02/12/docker-no-docker.html"><![CDATA[<p><a href="https://habr.com/ru/articles/881428/">https://habr.com/ru/articles/881428/</a></p>

<p>Технологии контейнеризации, возможно, как и у большинства читателей данной статьи, плотно засели в моей голове. И казалось бы, просто пиши Dockerfile и не выпендривайся. Но всегда же хочется узнавать что-то новое и углубляться в уже освоенные темы. По этой причине я решил разобратьсяв реализации контейнеров в ОС на базе ядра linux и в последствие создать свой «контейнер» через cmd.</p>

<h2 id="на-ком-держатся-контейнеры-в-linux">На ком держатся контейнеры в Linux?</h2>

<p>Для начала необходимо понять на чем именно основана технология контейнеризации. В ядре Linux существуют два механизма: namespace (пространство имен), cgroups (control groups). Они обеспечивают изоляцию и масштабируемость, за которые мы все так любим контейнеры. Давайте разберем по порядку оба механизма.</p>

<h3 id="namespace">Namespace</h3>

<p>Пространства имен позволяют нам изолировать ресурсы системы между процессами. С их помощью мы можем создать отдельную виртуальную систему, при этом формально находясь в хостовой. Возможно, данное краткое пояснение не особо просветило Вас, поэтому давайте взглянем на пример:</p>

<p>Рассмотрим контейнер поднятый из образа alpine. Запустим его и интерактивную оболочку в нем:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run -it alpine /bin/sh
</code></pre></div></div>

<p>Теперь создадим новый процесс в контейнере, и проверим вывод команды ps:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sleep 1000 &amp;
ps -a
</code></pre></div></div>

<p>Получаем:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PID   USER     TIME  COMMAND
    1 root      0:00 /bin/sh
   29 root      0:00 sleep 1000
   30 root      0:00 ps -a
</code></pre></div></div>

<p>Обратите внимание, что PID процесса равен 29. Теперь попробуем найти этот же процесс, но на хостовой машине. Для этого определим ID контейнера и воспользуемся командой для отображения процессов, запущенных внутри docker:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker top &lt;ID контейнера&gt;
</code></pre></div></div>

<p>В результате получаем:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UID     PID       PPID      C    STIME    TTY      TIME        CMD
root    172147    172124    0    Feb05    pts/0    00:00:00    /bin/sh
root    173602    172147    0    Feb05    pts/0    00:00:00    sleep 1000
</code></pre></div></div>

<p>Обратим внимание на 2 столбца: PID и PPID(parent PID). Они указывают на PID самого процесса и родительского, но уже в хостовой системе. Давайте проверим это:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps aux | grep -E '173602|172147'
</code></pre></div></div>

<p>Получаем:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root      172147  0.0  0.0   1736   908 pts/0    Ss+  Feb05   0:00 /bin/sh
root      173602  0.0  0.0   1624   980 pts/0    S    Feb05   0:00 sleep 1000
</code></pre></div></div>

<p>Что и требовалось доказать! Если подытожить, то можно сделать вывод, что контейнер ничего не знает про хостовую машину. Он считает, что является самостоятельной системой. Однако, в действительности все процессы запущены на хосте, просто они находятся в пространстве имен данного контейнера. Это и создает иллюзию отдельной независимой системы.</p>

<p>Надеюсь, данный пример чуть прояснил ситуацию с namespace. В нем мы разобрали один из 8 видов пространств имен. Теперь хотелось бы кратко пройтись по каждому:</p>

<ol>
  <li>Mount - изоляция точек монтирования файловой системы. Позволяет установить свою иерархию фс;</li>
  <li>UTS - изоляция имени хоста. Позволяет для каждого контейнера указать свое хостовое имя;</li>
  <li>PID - изоляция идентификаторов процессов. Позволяет создавать отдельное дерево процессов;</li>
  <li>Network - изоляция сетевых интерфейсов, таблиц маршрутизации;</li>
  <li>IPC - изоляция IPC(межпроцессные взаимодействия);</li>
  <li>User - изоляция пользователей системы. Позволяет создавать отдельных пользователей для каждого контейнера, в том числе и root;</li>
  <li>Cgroup - изоляция доступа к cgroup. Позволяет ограничивать ресурсы контейнера и предотвращает вмешательства других контейнеров;</li>
  <li>Time - изоляция системного времени.</li>
</ol>

<p>Для создания нового namespace в Linux существует команда unshare. С ней мы чуть позже познакомимся ближе.</p>

<h3 id="cgroups">Cgroups</h3>

<p>Control groups - механизм ядра Linux, позволяющий управлять ресурсами процессов. С его помощью можно ограничить и изолировать использование CPU, памяти, сети, диска.</p>

<p>Существует две версии cgoups: v1 и v2. В большинстве современных систем вы встретите вторую версию, которая используется в работе systemd. Основное отличие версий в построении дерева ограничений. В первой версии создавались узлы для каждого вида ограничений, а в них уже добавлялись группы. Во второй версии для каждой группы свой узел, внутри которого все необходимые ограничения. Чтобы лучше понять, давайте взглянем на визуализацию деревьев v1 и v2:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#v1
/sys/fs/cgroup/
├── cpu
│   ├── group1/
│   │   ├── tasks
│   │   ├── cgroup.procs
│   │   ├── cpu.shares
│   │   └── ...
│   ├── group2/
│   │   ├── tasks
│   │   ├── cgroup.procs
│   │   ├── cpu.shares
│   │   └── ...
│   └── ...
├── memory
│   ├── group1/
│   │   ├── tasks
│   │   ├── cgroup.procs
│   │   ├── memory.limit_in_bytes
│   │   └── ...
│   ├── group2/
│   │   ├── tasks
│   │   ├── cgroup.procs
│   │   ├── memory.limit_in_bytes
│   │   └── ...
│   └── ...
└── ...

#v2
/sys/fs/cgroup/
├── group1/
│   ├── cgroup.procs
│   ├── cpu.max
│   ├── cpu.weight
│   ├── memory.current
│   ├── memory.max
│   └── ...
├── group2/
│   ├── cgroup.procs
│   ├── cpu.max
│   ├── cpu.weight
│   ├── memory.current
│   ├── memory.max
│   └── ...
└── ...
</code></pre></div></div>

<p>Теперь давайте взглянем на работу cgroup на примере контейнера docker. Для начала запустим контейнер, ограничив его ресурсы(2 ядра и 512МБ):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run -d --cpus="2" --memory="512m" nginx
</code></pre></div></div>

<p>Далее найдем группу для этого контейнера, воспользовавшись командой find :</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>find /sys/fs/cgroup -name '*&lt;ID контейнера&gt;*'
</code></pre></div></div>

<p>Далее проверим содержание файлов cpu.max и memory.max в найденной директории:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># cpu.max
200000 100000

# memory.max
536870912
</code></pre></div></div>

<p>Что и требовалось доказать!</p>

<h2 id="создание-контейнера-без-docker">Создание контейнера без docker</h2>

<p>Мы разобрались с основной необходимой нам теорией. Теперь перейдем к практике и прибегнем к волшебству командной строки.</p>

<p>Для начала создадим структуру файловой системы контейнера, установим busybox в директорию /bin :</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Создаем корневую дирекотрию контейнера и переходим в нее
mkdir ~/container &amp;&amp; cd ~/container
# Создаем основные системные директории и переходим в /bin
mkdir -p ./{proc,sys,dev,tmp,bin,root,etc} &amp;&amp; cd bin
# Устанавливаем busybox
wget https://www.busybox.net/downloads/binaries/1.35.0-x86_64-linux-musl/busybox
# Выдаем право на исполнение
chmod +x busybox
# Создаем симлинки для всех команд, которые есть в busybox
./busybox --list | xargs -I {} ln -s busybox {}
# Возвращаемся в корневую директорию контейнера
cd ~/container
# Добавляем переменную PATH в файл /etc/profile
echo 'export PATH=/bin' &gt; ~/container/etc/profile
</code></pre></div></div>

<p>Также добавим в файлы /etc/passwd и /etc/group, чтобы внутри изолированной системы мы были рутом:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>echo "root:x:0:0:root:/root:/bin/sh" &gt; ~/container/etc/passwd
echo "root:x:0:" &gt; ~/container/etc/group
</code></pre></div></div>

<p>Далее смонтируем системные директории:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Монтируем устройства, используя уже существующие
sudo mount --bind /dev ~/container/dev
# Монтируем процессы
sudo mount -t proc none ~/container/proc
# Монтируем файловую систему sysfs
sudo mount -t sysfs none ~/container/sys
# Монтируем файловую систему tmpfs
sudo mount -t tmpfs none ~/container/tmp
</code></pre></div></div>

<p>Примечание: для того чтобы потом размонтировать можно воспользоваться командой:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sudo umount ~/container/{proc,sys,dev,tmp}
</code></pre></div></div>

<p>Мы подготовили файловую систему для нашего контейнера. Теперь перейдем к созданию изоляции. Для этого мы воспользуемся командой:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>unshare -f -p -m -n -i -u -U --map-root-user --mount-proc=./proc \
    /bin/chroot ~/container /bin/sh -c "source /etc/profile &amp;&amp; exec /bin/sh"
</code></pre></div></div>

<p>Давайте разберем ее подробнее:</p>

<ul>
  <li>-f - fork. Создаем новый процесс для изоляции от родительского;</li>
  <li>-p - PID namespace;</li>
  <li>-m - mount namespace;</li>
  <li>-n - Network namespace;</li>
  <li>-i - IPC namespace;</li>
  <li>-u - UTS namespace;</li>
  <li>-U - User namespace;</li>
  <li>–map-root-user - маппинг uid и gid активного пользователя на root внутри контейнера;</li>
  <li>-mount-proc - монтируем proc внутри контейнера;</li>
  <li>/bin/chroot ~/container - меняем корневую директорию;</li>
  <li>/bin/sh -c “source /etc/profile &amp;&amp; exec /bin/sh” - запускаем shell и исполняем команду, которая применит файл /etc/profile и запустит интерактивный shell.</li>
</ul>

<p>Отлично! Мы получили свой контейнер. Теперь осталось ограничить ресурсы. Для этого откроем новую сессию на хосте и выполним ряд действий:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Создаем новую группу. В моей системе используется cgroups v2, поэтому
# директория автоматически будет настроена для работы с ресурсами
sudo mkdir /sys/fs/cgroup/my_container
# Записываем ограничение на 2 ядра процессора
echo "200000 100000" | sudo tee /sys/fs/cgroup/my_container/cpu.max
# Выделяем максимум 512MB памяти
echo 536870912 | sudo tee /sys/fs/cgroup/my_container/memory.max
</code></pre></div></div>

<p>Далее необходимо определить PID контейнера, для этого воспользуемся командой:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps aux | grep -E '/bin/sh$'
</code></pre></div></div>

<p>Берем PID из второго столбца и добавляем в файл cgroup.procs :</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>echo &lt;PID&gt; | sudo tee /sys/fs/cgroup/my_container/cgroup.procs
</code></pre></div></div>

<p>На этом основные настройки закончены. Мы создали изолированную систему и добавили ограничение ресурсов. Но хотелось бы сделать ее чуть более функциональной, для этого настроим виртуальную сеть между хостом и контейнером:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Создаем пару виртуальных интерфейсов
sudo ip link add veth-host type veth peer name veth-container
# Поднимаем интерфейс на хосте
sudo ip link set veth-host up
# Назанчаем любой свободный адрес в вашей сети на интерфейс хоста
# Я использую 192.168.1.123/24
sudo ip addr add 192.168.1.123/24 dev veth-host
# Перемещаем veth-container в пространство имен контейнера
# Здесь нужно указать PID контейнера, который использовали до этого
sudo ip link set veth-container netns &lt;PID&gt;
# Поднимаем интерфейс внутри контейнер
sudo nsenter --net=/proc/&lt;PID&gt;/ns/net ip link set veth-container up
# Назанчаем любой свободный адрес в вашей сети на интерфейс контейнера
# Я использую 192.168.1.124/24
sudo nsenter --net=/proc/&lt;PID&gt;/ns/net ip addr add 192.168.1.124/24 dev veth-container
# Настраиваем шлюз по умолчанию для маршрутизации трафика
sudo nsenter --net=/proc/&lt;PID&gt;/ns/net ip route add default via 192.168.1.123
</code></pre></div></div>

<p>Мы подняли все нужные интерфейсы. Теперь необходимо настроить маршрутизацию:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Разрешаем пересылку пакетов
echo 1 | sudo tee /proc/sys/net/ipv4/ip_forward
# Добавляем правило NAT для маскардинга для исходящих пакетов из сети
# 192.168.1.0/24 через интерфейс который смотрит во внешнюю сеть. У меня это enp3s0.
# Маскардинг маскирует пакеты исходящие их контейнера так, чтобы они выглядели,
# как пакеты отправленные с хоста
sudo iptables -t nat -A POSTROUTING -s 192.168.1.0/24 -o enp3s0 -j MASQUERADE
# Добавляем парвило на разрешение форвардинга пакетов
sudo iptables -A FORWARD -s 192.168.1.0/24 -o enp3s0 -j ACCEPT
# Добавляем парвило, разрешающее входящие пакеты.
sudo iptables -A FORWARD -d 192.168.1.0/24 -m state --state RELATED,ESTABLISHED -j ACCEPT
</code></pre></div></div>

<p>Отлично! Мы создали свой первый контейнер. Понятно, что в нем еще много чего можно настроить, тот же DNS, который сейчас не работает. Но это уже каждый сам решит, как с этим играть.</p>

<p>Спасибо за прочтение!</p>]]></content><author><name></name></author><category term="habr" /><category term="linux,namespace,контейнер,cgroups,docker,контейнеризация,cgroup" /><category term="v2" /><summary type="html"><![CDATA[https://habr.com/ru/articles/881428/]]></summary></entry><entry><title type="html">Обвиваем YouTube змеем, или как смотреть и скачивать видео с YouTube без VPN на чистом Python-е. Часть 1</title><link href="http://localhost:4000/habr/2024/12/27/anti-dpi.html" rel="alternate" type="text/html" title="Обвиваем YouTube змеем, или как смотреть и скачивать видео с YouTube без VPN на чистом Python-е. Часть 1" /><published>2024-12-27T00:00:00+03:00</published><updated>2024-12-27T00:00:00+03:00</updated><id>http://localhost:4000/habr/2024/12/27/anti-dpi</id><content type="html" xml:base="http://localhost:4000/habr/2024/12/27/anti-dpi.html"><![CDATA[<p><a href="https://habr.com/ru/articles/870110/">https://habr.com/ru/articles/870110/</a></p>

<p>Современный мир пресыщен различной информацией, и в наше непростое время её важно уметь не только находить и сохранять. Многие наверняка заметили, что в на YouTube, кроме мусора, котиков и прочих бесполезных вещей (которые мы иногда не прочь посмотреть) есть масса полезного материала по самым различным темам. И иногда этот материал неплохо было бы сохранить себе на будущее, чтобы не зависеть от переменчивых настроений в мире.</p>

<p>В этой статье я хочу рассказать, как можно скачивать видео, аудио (1 часть статьи), плейлисты и целые каналы с YouTube (2 часть статьи) без использования VPN и на чистом Python-е. Сразу оговорка: VPN нам не понадобится, но мы сделаем собственное средство, которое будет решать “проблему с устаревшим и изношенным оборудованием <a href="https://ru.wikipedia.org/wiki/Google_Global_Cache">Google Global Cache</a>” (вы поняли, о чём я). Я думаю это средство будет особенно актуально сегодня, когда у многих россиян YouTube почти или совсем не работает.</p>

<h4 id="зачем-изобретать-велосипед-и-почему-python">Зачем изобретать велосипед и почему Python?</h4>

<ol>
  <li>
    <p>Я программирую на Python, и о чём мне рассказывать как не о нём?</p>
  </li>
  <li>
    <p>У python есть широкие возможности для создания таких инструментов и готовые библиотеки с большим функционалом.</p>
  </li>
  <li>
    <p>Существуют такие замечательные утилиты, как yt-dlp и youtube-dl, но они подходят, когда нужно по-быстрому что-то скачать. Тонкая настройка и скачивание, например целого канала, вещь довольно геморройная (если, нет - поправьте меня), так как постоянно нужно что-то гуглить и знать многочисленные ключи и параметры.</p>
  </li>
  <li>
    <p>Существуют и другие десктопные программы, мобильные приложения и сайты, но как правило они или платные, или напичканы рекламой, или ограничены в функционале, или даже всё вместе.</p>
  </li>
  <li>
    <p>Ну и по-моему, собственный скрипт всегда удобнее любой навороченной утилиты, да и глаз радуется на красивые строчки в редакторе)</p>
  </li>
</ol>

<h2 id="этап-1-обходим-блок-ой-решаем-проблему-с-устаревшим-и-изношенным-оборудованием">Этап 1. Обходим блок… ой!.. Решаем проблему с устаревшим и изношенным оборудованием.</h2>

<p>Почему YouTube у нас работает не так, как надо? Конечно, все знают, что дело здесь не в оборудовании, а в том, что провайдеры используют такую зловредную (в данном случае) вещь, как <a href="https://ru.wikipedia.org/wiki/Deep_packet_inspection">DPI</a> (англ. Deep Packet Inspection «глубокая инспекция пакетов»)</p>

<h3 id="how-it-works">How it works</h3>

<p>Большинство DPI работает так. Когда вы пытаетесь получить доступ к заблокированному веб-сайту, DPI отправляет вам HTTP 302 Redirect, и делает это быстрее, чем веб-сайт к которому вы обращаетесь. В отличие от брандмауэров, Deep Packet Inspection анализирует не только заголовки пакетов, но и содержимое запросов, что позволяет интернет-провайдерам и государственным органам ограничивать доступ к запрещенным ресурсам, выявлять вторжения в сеть и останавливать распространение компьютерных вирусов.</p>

<p>Это довольно упрощенная модель пассивного DPI, но так как целью этой статьи не является подробное ознакомление с этой темой, да и я, честно признаюсь, не большой специалист в этом, то думаю этого достаточно для понимания того, что происходит. Если вас интересует эта тема, то можете почитать <a href="https://ru.wikipedia.org/wiki/Deep_packet_inspection">здесь</a> и <a href="https://habr.com/ru/articles/111054/">здесь</a>.</p>

<h3 id="что-же-делать">Что же делать</h3>

<p>Но, на каждый болт найдется своя гайка, и есть способы обойти эту вещь (хотя они не всегда действуют). Многие наверняка слышали про <a href="https://github.com/ValdikSS/GoodbyeDPI">GoodbyeDPI</a> или <a href="https://github.com/bol-van/zapret">zapret</a>. Это замечательные инструменты, но раз уж мы говорим про Python, то давайте сделаем свой аналог на Python. Поискав немного, я нашел <a href="https://github.com/theo0x0/nodpi">nodpi</a>, который использует только python.</p>

<h3 id="как-это-работает">Как это работает</h3>

<p>nodpi создает прокси-сервер (socket), через который мы гоним трафик. Все исходящие соединения он случайным образом фрагментирует, тем самым сбивая с толку DPI.</p>

<h4 id="плюсы">Плюсы</h4>

<ol>
  <li>
    <p>Это написано на Python!</p>
  </li>
  <li>
    <p>Просто, дёшево, сердито и работает для большинства провайдеров</p>
  </li>
  <li>
    <p>Не требуются привилегии администратора, как, например у GoodbyeDPI</p>
  </li>
  <li>
    <p>Прокси-сервер можно настроить только в некоторых приложениях и не гнать через него весь трафик системы.</p>
  </li>
</ol>

<h4 id="минусы">Минусы</h4>

<ol>
  <li>
    <p>Простота - не всегда качество, поэтому гарантия не даётся</p>
  </li>
  <li>
    <p>Работает только для TCP</p>
  </li>
  <li>
    <p>Не поможет, если сайт забанен по IP</p>
  </li>
</ol>

<h2 id="пишем-нет---переписываем-код">Пишем…, нет - переписываем код</h2>

<p>Программа написана довольно небрежно, словно у разработчика слипались глаза и он залил код прямо так, не разбираясь, и поэтому я приведу причёсанный код и заодно немного расскажу, что он делает.</p>

<p>Для начала создадим текстовый файл <code class="language-plaintext highlighter-rouge">blacklist.txt</code> и добавим туда домены, блокировку которых хотим обходить. Для YouTube это:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>youtube.com
youtu.be
yt.be
googlevideo.com
ytimg.com
ggpht.com
gvt1.com
youtube-nocookie.com
youtube-ui.l.google.com
youtubeembeddedplayer.googleapis.com
youtube.googleapis.com
youtubei.googleapis.com
yt-video-upload.l.google.com
wide-youtube.l.google.com
</code></pre></div></div>

<p>Теперь создадим файл <code class="language-plaintext highlighter-rouge">nodpi.py</code> и добавим в него следующий код</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">random</span>
<span class="kn">import</span> <span class="n">asyncio</span>

<span class="n">BLOCKED</span> <span class="o">=</span> <span class="p">[</span><span class="n">line</span><span class="p">.</span><span class="nf">rstrip</span><span class="p">().</span><span class="nf">encode</span><span class="p">()</span> <span class="k">for</span> <span class="n">line</span> <span class="ow">in</span> <span class="nf">open</span><span class="p">(</span><span class="sh">'</span><span class="s">blacklist.txt</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">r</span><span class="sh">'</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">'</span><span class="s">utf-8</span><span class="sh">'</span><span class="p">)]</span>
<span class="n">TASKS</span> <span class="o">=</span> <span class="p">[]</span>
</code></pre></div></div>

<p>Что мы здесь делаем:</p>

<ol>
  <li>
    <p>Импортируем необходимые библиотеки</p>
  </li>
  <li>
    <p>Создаём список заблокированных доменов</p>
  </li>
  <li>
    <p>Создаём список, в котором будем хранить задачи передачи данных между клиентом и сервером</p>
  </li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">(</span><span class="n">host</span><span class="p">,</span> <span class="n">port</span><span class="p">):</span>

    <span class="n">server</span> <span class="o">=</span> <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">start_server</span><span class="p">(</span><span class="n">new_conn</span><span class="p">,</span> <span class="n">host</span><span class="p">,</span> <span class="n">port</span><span class="p">)</span>
    <span class="k">await</span> <span class="n">server</span><span class="p">.</span><span class="nf">serve_forever</span><span class="p">()</span>
</code></pre></div></div>

<p>Создаём входную точку - асинхронную функцию, которая запускает сокет-сервер на указанном хосте и порту и используем new_conn (ниже) для обработки новых соединений.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">pipe</span><span class="p">(</span><span class="n">reader</span><span class="p">,</span> <span class="n">writer</span><span class="p">):</span>

    <span class="k">while</span> <span class="ow">not</span> <span class="n">reader</span><span class="p">.</span><span class="nf">at_eof</span><span class="p">()</span> <span class="ow">and</span> <span class="ow">not</span> <span class="n">writer</span><span class="p">.</span><span class="nf">is_closing</span><span class="p">():</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">writer</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="k">await</span> <span class="n">reader</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">1500</span><span class="p">))</span>
            <span class="k">await</span> <span class="n">writer</span><span class="p">.</span><span class="nf">drain</span><span class="p">()</span>
        <span class="k">except</span><span class="p">:</span>
            <span class="k">break</span>

    <span class="n">writer</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
</code></pre></div></div>

<p>Создаём асинхронную функцию, которая читает данные из reader и записывает их в writer до тех пор, пока не достигнут конец или соединение не закрыто.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">new_conn</span><span class="p">(</span><span class="n">local_reader</span><span class="p">,</span> <span class="n">local_writer</span><span class="p">):</span>

    <span class="n">http_data</span> <span class="o">=</span> <span class="k">await</span> <span class="n">local_reader</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">1500</span><span class="p">)</span>

    <span class="k">try</span><span class="p">:</span>
        <span class="nb">type</span><span class="p">,</span> <span class="n">target</span> <span class="o">=</span> <span class="n">http_data</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="se">\r\n</span><span class="sh">"</span><span class="p">)[</span><span class="mi">0</span><span class="p">].</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s"> </span><span class="sh">"</span><span class="p">)[</span><span class="mi">0</span><span class="p">:</span><span class="mi">2</span><span class="p">]</span>
        <span class="n">host</span><span class="p">,</span> <span class="n">port</span> <span class="o">=</span> <span class="n">target</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">:</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">except</span><span class="p">:</span>
        <span class="n">local_writer</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
        <span class="k">return</span>

    <span class="k">if</span> <span class="nb">type</span> <span class="o">!=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">CONNECT</span><span class="sh">"</span><span class="p">:</span>
        <span class="n">local_writer</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
        <span class="k">return</span>

    <span class="n">local_writer</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="sa">b</span><span class="sh">'</span><span class="s">HTTP/1.1 200 OK</span><span class="se">\n\n</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">await</span> <span class="n">local_writer</span><span class="p">.</span><span class="nf">drain</span><span class="p">()</span>

    <span class="k">try</span><span class="p">:</span>
        <span class="n">remote_reader</span><span class="p">,</span> <span class="n">remote_writer</span> <span class="o">=</span> <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">open_connection</span><span class="p">(</span><span class="n">host</span><span class="p">,</span> <span class="n">port</span><span class="p">)</span>
    <span class="k">except</span><span class="p">:</span>
        <span class="n">local_writer</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
        <span class="k">return</span>

    <span class="k">if</span> <span class="n">port</span> <span class="o">==</span> <span class="sa">b</span><span class="sh">'</span><span class="s">443</span><span class="sh">'</span><span class="p">:</span>
        <span class="k">await</span> <span class="nf">fragemtn_data</span><span class="p">(</span><span class="n">local_reader</span><span class="p">,</span> <span class="n">remote_writer</span><span class="p">)</span>

    <span class="n">TASKS</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">asyncio</span><span class="p">.</span><span class="nf">create_task</span><span class="p">(</span><span class="nf">pipe</span><span class="p">(</span><span class="n">local_reader</span><span class="p">,</span> <span class="n">remote_writer</span><span class="p">)))</span>
    <span class="n">TASKS</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">asyncio</span><span class="p">.</span><span class="nf">create_task</span><span class="p">(</span><span class="nf">pipe</span><span class="p">(</span><span class="n">remote_reader</span><span class="p">,</span> <span class="n">local_writer</span><span class="p">)))</span>
</code></pre></div></div>

<ol>
  <li>
    <p>Создаём асинхронную функцию, которая обрабатывает новое соединение.</p>
  </li>
  <li>
    <p>Читаем HTTP-заголовки и извлекаем тип запроса и целевой адрес.</p>
  </li>
  <li>
    <p>Если тип запроса не CONNECT, закрываем соединение.</p>
  </li>
  <li>
    <p>Отправляем ответ HTTP/1.1 200 OK клиенту.</p>
  </li>
  <li>
    <p>Открываем соединение с целевым сервером.</p>
  </li>
  <li>
    <p>Если порт равен 443 (порт HTTPS), вызываем функцию <code class="language-plaintext highlighter-rouge">fragemtn_data</code> (ниже)</p>
  </li>
  <li>
    <p>Создаем задачи для передачи данных между клиентом и сервером.</p>
  </li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">fragemtn_data</span><span class="p">(</span><span class="n">local_reader</span><span class="p">,</span> <span class="n">remote_writer</span><span class="p">):</span>

    <span class="n">head</span> <span class="o">=</span> <span class="k">await</span> <span class="n">local_reader</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="n">data</span> <span class="o">=</span> <span class="k">await</span> <span class="n">local_reader</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">1500</span><span class="p">)</span>
    <span class="n">parts</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="k">if</span> <span class="nf">all</span><span class="p">([</span><span class="n">data</span><span class="p">.</span><span class="nf">find</span><span class="p">(</span><span class="n">site</span><span class="p">)</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span> <span class="k">for</span> <span class="n">site</span> <span class="ow">in</span> <span class="n">BLOCKED</span><span class="p">]):</span>
        <span class="n">remote_writer</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">head</span> <span class="o">+</span> <span class="n">data</span><span class="p">)</span>
        <span class="k">await</span> <span class="n">remote_writer</span><span class="p">.</span><span class="nf">drain</span><span class="p">()</span>

        <span class="k">return</span>

    <span class="k">while</span> <span class="n">data</span><span class="p">:</span>
        <span class="n">part_len</span> <span class="o">=</span> <span class="n">random</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">))</span>
        <span class="n">parts</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nb">bytes</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="sh">"</span><span class="s">1603</span><span class="sh">"</span><span class="p">)</span> <span class="o">+</span> <span class="nf">bytes</span><span class="p">([</span><span class="n">random</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">255</span><span class="p">)])</span> <span class="o">+</span> <span class="nf">int</span><span class="p">(</span>
            <span class="n">part_len</span><span class="p">).</span><span class="nf">to_bytes</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="n">byteorder</span><span class="o">=</span><span class="sh">'</span><span class="s">big</span><span class="sh">'</span><span class="p">)</span> <span class="o">+</span> <span class="n">data</span><span class="p">[</span><span class="mi">0</span><span class="p">:</span><span class="n">part_len</span><span class="p">])</span>

        <span class="n">data</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="n">part_len</span><span class="p">:]</span>

    <span class="n">remote_writer</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="sa">b</span><span class="sh">''</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">parts</span><span class="p">))</span>
    <span class="k">await</span> <span class="n">remote_writer</span><span class="p">.</span><span class="nf">drain</span><span class="p">()</span>
</code></pre></div></div>

<ol>
  <li>
    <p>Создаём асинхронную функцию, которая фрагментирует данные перед отправкой на сервер.</p>
  </li>
  <li>
    <p>Читаем заголовок и данные.</p>
  </li>
  <li>
    <p>Если данные не из заблокированных доменов, отправляем их без изменений.</p>
  </li>
  <li>
    <p>В противном случае, разбиваем данные на случайные части и добавляем заголовки перед отправкой. Это помогает обойти блокировку DPI, так как фрагментированные данные выглядят как случайные пакеты.</p>
  </li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">main</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="sh">'</span><span class="s">127.0.0.1</span><span class="sh">'</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">8881</span><span class="p">))</span>
</code></pre></div></div>

<p>Запускаем сервер на локальном <code class="language-plaintext highlighter-rouge">127.0.0.1</code> и слушаем порт <code class="language-plaintext highlighter-rouge">8881</code></p>

<p>Всё, можно пользоваться!</p>

<p>Давайте проверим. Если у вас Firefox, то заходим в Настройки → Настройки сети и прописываем IP и порт:</p>

<p>Открываем YouTube и убеждаемся, что все работает или не работает :)</p>

<h3 id="этап-2-скачиваем">Этап 2. Скачиваем!</h3>

<p>Установка необходимого инструментария</p>

<p>Для начала установим библиотеку <code class="language-plaintext highlighter-rouge">pytubefix</code> командой:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip install pytubefix
</code></pre></div></div>

<p>Также нам понадобится <code class="language-plaintext highlighter-rouge">ffmpeg</code>, для объединения аудио и видео (если, конечно, вы хотите скачивать в высоком качестве). Почему? Дело в том, что YouTube дает видео с аудио в одном потоке только в разрешении 360p (раньше было 720p), и чтобы скачать видео, например в 1080p, нужно сначала скачать видео в 1080p, потом скачать аудио, и затем все это объединить конвертером. ffmpeg существует и для Windows. Проверенную сборку можно скачать с моего Google Диска, но вы можете найти ее и самостоятельно в интернете.</p>

<h3 id="простая-загрузка-видео">Простая загрузка видео</h3>

<p>Давайте попробуем скачать какое-нибудь видео. Создайте файл <code class="language-plaintext highlighter-rouge">yt_downloader.py</code> и вставьте в него код:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pytubefix</span> <span class="kn">import</span> <span class="n">YouTube</span>
<span class="kn">from</span> <span class="n">pytubefix.cli</span> <span class="kn">import</span> <span class="n">on_progress</span>

<span class="n">url</span> <span class="o">=</span> <span class="sh">"</span><span class="s">https://www.youtube.com/watch?v=xxxxxxxxxxx</span><span class="sh">"</span>

<span class="n">video</span> <span class="o">=</span> <span class="nc">YouTube</span><span class="p">(</span>
    <span class="n">proxies</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">http</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">http://127.0.0.1:8881</span><span class="sh">"</span><span class="p">,</span>
             <span class="sh">"</span><span class="s">https</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">http://127.0.0.1:8881</span><span class="sh">"</span><span class="p">},</span>
    <span class="n">url</span><span class="o">=</span><span class="n">url</span><span class="p">,</span>
    <span class="n">on_progress_callback</span><span class="o">=</span><span class="n">on_progress</span><span class="p">,</span>
<span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">Title:</span><span class="sh">'</span><span class="p">,</span> <span class="n">yt</span><span class="p">.</span><span class="n">title</span><span class="p">)</span>

<span class="n">stream</span> <span class="o">=</span> <span class="n">video</span><span class="p">.</span><span class="n">streams</span><span class="p">.</span><span class="nf">get_highest_resolution</span><span class="p">()</span>
<span class="n">stream</span><span class="p">.</span><span class="nf">download</span><span class="p">()</span>
</code></pre></div></div>

<p>Давайте разберём, что делает этот код. Сначала мы импортируем класс YouTube, с помощью которого мы скачиваем видео, и функцию <code class="language-plaintext highlighter-rouge">on_progress</code>. Она нужна для того, чтобы во время скачивания отображался прогрессбар загрузки. Согласитесь, что так гораздо удобнее? При желании можно написать свою функцию-callback. Она должна иметь аргументы <code class="language-plaintext highlighter-rouge">(stream: Stream, chunk: bytes, bytes_remaining: int)</code></p>

<p>В переменную <code class="language-plaintext highlighter-rouge">url</code> вставьте свою ссылку для скачивания, так как я заменил id видео иксами. Далее мы создаем экземпляр класса YouTube и передаем ему словарь с адресом нашего прокси-сервера, url и callback. Все аргументы, кроме url являются необязательными. Также стоит отметить, что callback вызывается каждый раз, после скачивания нового чанка (pytubefix скачивает видео кусками - чанками).</p>

<p>Далее мы выводим на экран заголовок (название) видео. Также можно узнать дату публикации, длину видео (в секундах), описание, количество просмотров и автора. Размер файла можно узнать с помощью <code class="language-plaintext highlighter-rouge">stream.filesize</code>, так как он зависит от выбранного потока.</p>

<p>В строке <code class="language-plaintext highlighter-rouge">stream = video.streams.get_highest_resolution()</code> мы получаем список потоков и сортируем его по разрешению (качеству) видео. Тут я хочу остановиться и рассказать немного подробнее. У каждого видео на YouTube есть несколько десятков потоков, каждый из которых представляет собой одно видео, одно аудио или все сразу в разных форматах, кодировках и качестве. Так, для моего видео список потоков выглядит примерно так:</p>

<p>Вы можете посмотреть полный список потоков для своего видео, просто выведя на экран <code class="language-plaintext highlighter-rouge">print(video.streams)</code>, но как правило это не требуется, так как библиотека предоставляет средства для их сортировки и фильтрации. В нашем случае это get_highest_resolution(). Эта функция возвращает поток с самым лучшим разрешением, который содержит и аудио, и видео. Как я уже объяснял, таким разрешением окажется 360p, потому что YouTube дает видео с аудио в одном потоке только в разрешении 360p (раньше было 720p), и чтобы скачать видео, например в 1080p, нужно сначала скачать видео в 1080p, потом скачать аудио, и затем все это объединить конвертером. Скрипт, который скачивает видео в более высоком качестве, мы напишем чуть позднее.</p>

<p>Ну и наконец, в строке stream.download() мы скачиваем видео. При скачивании будет отображаться прогрессбар, и вы увидите процесс загрузки.</p>

<p>Запустите скрипт и посмотрите результат. <strong>Не забудьте перед этим запустить нашу программу для обхода DPI!</strong></p>

<h3 id="скачиваем-видео-в-высоком-разрешении">Скачиваем видео в высоком разрешении</h3>

<p>Видео в 360p, это, конечно, не очень. Давайте расширим нашу программу. Если вы еще не скачали <code class="language-plaintext highlighter-rouge">ffmpeg</code>, сделайте это. Без него скачивание в другом расширении невозможно. Предполагается, что <code class="language-plaintext highlighter-rouge">ffmpeg</code> лежит в той же папке, что и скрипт.</p>

<p>Создайте файл <code class="language-plaintext highlighter-rouge">yt_downloader_2.py</code> и добавьте в него следующий код:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>

<span class="kn">from</span> <span class="n">pytubefix</span> <span class="kn">import</span> <span class="n">YouTube</span>
<span class="kn">from</span> <span class="n">pytubefix.cli</span> <span class="kn">import</span> <span class="n">on_progress</span>

<span class="n">url</span> <span class="o">=</span> <span class="sh">"</span><span class="s">https://www.youtube.com/watch?v=xxxxxxxxxxx</span><span class="sh">"</span>


<span class="k">def</span> <span class="nf">combine</span><span class="p">(</span><span class="n">audio</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">video</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">output</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>

    <span class="k">if</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">exists</span><span class="p">(</span><span class="n">output</span><span class="p">):</span>
        <span class="n">os</span><span class="p">.</span><span class="nf">remove</span><span class="p">(</span><span class="n">output</span><span class="p">)</span>

    <span class="n">code</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">system</span><span class="p">(</span>
        <span class="sa">f</span><span class="sh">'</span><span class="s">.</span><span class="se">\\</span><span class="s">ffmpeg.exe -i </span><span class="sh">"</span><span class="si">{</span><span class="n">video</span><span class="si">}</span><span class="sh">"</span><span class="s"> -i </span><span class="sh">"</span><span class="si">{</span><span class="n">audio</span><span class="si">}</span><span class="sh">"</span><span class="s"> -c copy </span><span class="sh">"</span><span class="si">{</span><span class="n">output</span><span class="si">}</span><span class="sh">"'</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">code</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nc">SystemError</span><span class="p">(</span><span class="n">code</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">download</span><span class="p">(</span><span class="n">url</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>

    <span class="n">yt</span> <span class="o">=</span> <span class="nc">YouTube</span><span class="p">(</span>
        <span class="n">proxies</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">http</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">http://127.0.0.1:8881</span><span class="sh">"</span><span class="p">,</span>
                 <span class="sh">"</span><span class="s">https</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">http://127.0.0.1:8881</span><span class="sh">"</span><span class="p">},</span>
        <span class="n">url</span><span class="o">=</span><span class="n">url</span><span class="p">,</span>
        <span class="n">on_progress_callback</span><span class="o">=</span><span class="n">on_progress</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="n">video_stream</span> <span class="o">=</span> <span class="n">yt</span><span class="p">.</span><span class="n">streams</span><span class="p">.</span>\
        <span class="nf">filter</span><span class="p">(</span><span class="nb">type</span><span class="o">=</span><span class="sh">'</span><span class="s">video</span><span class="sh">'</span><span class="p">).</span>\
        <span class="nf">order_by</span><span class="p">(</span><span class="sh">'</span><span class="s">resolution</span><span class="sh">'</span><span class="p">).</span>\
        <span class="nf">desc</span><span class="p">().</span><span class="nf">first</span><span class="p">()</span>

    <span class="n">audio_stream</span> <span class="o">=</span> <span class="n">yt</span><span class="p">.</span><span class="n">streams</span><span class="p">.</span>\
        <span class="nf">filter</span><span class="p">(</span><span class="n">mime_type</span><span class="o">=</span><span class="sh">'</span><span class="s">audio/mp4</span><span class="sh">'</span><span class="p">).</span>\
        <span class="nf">order_by</span><span class="p">(</span><span class="sh">'</span><span class="s">filesize</span><span class="sh">'</span><span class="p">).</span>\
        <span class="nf">desc</span><span class="p">().</span><span class="nf">first</span><span class="p">()</span>

    <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">Information:</span><span class="sh">'</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Title:</span><span class="sh">"</span><span class="p">,</span> <span class="n">yt</span><span class="p">.</span><span class="n">title</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Author:</span><span class="sh">"</span><span class="p">,</span> <span class="n">yt</span><span class="p">.</span><span class="n">author</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Date:</span><span class="sh">"</span><span class="p">,</span> <span class="n">yt</span><span class="p">.</span><span class="n">publish_date</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Resolution:</span><span class="sh">"</span><span class="p">,</span> <span class="n">video_stream</span><span class="p">.</span><span class="n">resolution</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Views:</span><span class="sh">"</span><span class="p">,</span> <span class="n">yt</span><span class="p">.</span><span class="n">views</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Length:</span><span class="sh">"</span><span class="p">,</span> <span class="nf">round</span><span class="p">(</span><span class="n">yt</span><span class="p">.</span><span class="n">length</span><span class="o">/</span><span class="mi">60</span><span class="p">),</span> <span class="sh">"</span><span class="s">minutes</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Filename of the video:</span><span class="sh">"</span><span class="p">,</span> <span class="n">video_stream</span><span class="p">.</span><span class="n">default_filename</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="s">Filesize of the video:</span><span class="sh">"</span><span class="p">,</span> <span class="nf">round</span><span class="p">(</span>
        <span class="n">video_stream</span><span class="p">.</span><span class="n">filesize</span> <span class="o">/</span> <span class="mi">1000000</span><span class="p">),</span> <span class="sh">"</span><span class="s">MB</span><span class="sh">"</span><span class="p">)</span>

    <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">Download video...</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">video_stream</span><span class="p">.</span><span class="nf">download</span><span class="p">()</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="se">\n</span><span class="s">Download audio...</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">audio_stream</span><span class="p">.</span><span class="nf">download</span><span class="p">()</span>

    <span class="nf">combine</span><span class="p">(</span><span class="n">audio_stream</span><span class="p">.</span><span class="n">default_filename</span><span class="p">,</span> <span class="n">video_stream</span><span class="p">.</span><span class="n">default_filename</span><span class="p">,</span>
            <span class="sa">f</span><span class="sh">'</span><span class="si">{</span><span class="n">yt</span><span class="p">.</span><span class="n">title</span><span class="si">}</span><span class="s">.mp4</span><span class="sh">'</span><span class="p">)</span>

<span class="nf">download</span><span class="p">(</span><span class="n">url</span><span class="p">)</span>
</code></pre></div></div>

<p>Что изменилось? Мы добавили функцию combine, которая отвечает за объединение видео и аудио. Мы делаем это командой <code class="language-plaintext highlighter-rouge">.\ffmpeg.exe -i "filename_video" -i "filename_audio" -c copy "output_filename"</code></p>

<p>В основном коде мы по отдельности вытаскиваем потоки видео и аудио. Здесь видео не содержит аудио, но зато оно в высоком качестве. Чтобы получить его, мы сначала фильтруем потоки по типу (видео) <code class="language-plaintext highlighter-rouge">filter(type='video')</code>,  затем сортируем по разрешению, затем сортируем по убыванию разрешения и берём первый поток. Аналогично с аудио. После этого мы выводим подробную информацию о скачиваемом видео и скачиваем аудио и видео по отдельности, после чего объединяем их с помощью ffmpeg. Ничего сложного!</p>

<p>Запускаем наш скрипт для обхода DPI и скрипт для скачивания (замените url на свой). Готово!</p>

<h3 id="как-узнать-список-доступных-разрешений">Как узнать список доступных разрешений</h3>

<p>Чтобы узнать, в каких разрешениях можно скачать данное видео, можно написать такую функцию:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">resolutions</span><span class="p">(</span><span class="n">video</span><span class="p">:</span> <span class="n">YouTube</span><span class="p">):</span>

    <span class="n">res</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="n">streams</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">video</span><span class="p">.</span><span class="n">streams</span><span class="p">.</span>\
                    <span class="nf">filter</span><span class="p">(</span><span class="nb">type</span><span class="o">=</span><span class="sh">'</span><span class="s">video</span><span class="sh">'</span><span class="p">).</span>\
                    <span class="nf">order_by</span><span class="p">(</span><span class="sh">'</span><span class="s">resolution</span><span class="sh">'</span><span class="p">).</span>\
                    <span class="nf">desc</span><span class="p">()</span>

    <span class="k">for</span> <span class="n">stream</span> <span class="ow">in</span> <span class="n">streams</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">stream</span><span class="p">.</span><span class="n">resolution</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">res</span><span class="p">:</span>
            <span class="n">res</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">stream</span><span class="p">.</span><span class="n">resolution</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">res</span>
</code></pre></div></div>

<p>Поток с выбранным разрешением можно получить следующим образом:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">res</span> <span class="o">=</span> <span class="sh">'</span><span class="s">1080p</span><span class="sh">'</span> <span class="c1"># например
</span><span class="n">stream</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">video</span><span class="p">.</span><span class="n">streams</span><span class="p">.</span>\
          <span class="nf">filter</span><span class="p">(</span><span class="n">resolution</span><span class="o">=</span><span class="n">res</span><span class="p">,</span> <span class="n">progressive</span><span class="o">=</span><span class="bp">False</span><span class="p">).</span><span class="nf">desc</span><span class="p">().</span><span class="nf">first</span><span class="p">()</span>
</code></pre></div></div>

<p>Если вы не используете <code class="language-plaintext highlighter-rouge">ffmpeg</code>, замените <code class="language-plaintext highlighter-rouge">progressive=False</code> на <code class="language-plaintext highlighter-rouge">True</code></p>

<h3 id="скачивание-аудио">Скачивание аудио</h3>

<p>Из предыдущего примера вы видели, как мы скачивали аудио:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">...</span>

<span class="n">audio_stream</span> <span class="o">=</span> <span class="n">yt</span><span class="p">.</span><span class="n">streams</span><span class="p">.</span>\
        <span class="nf">filter</span><span class="p">(</span><span class="n">mime_type</span><span class="o">=</span><span class="sh">'</span><span class="s">audio/mp4</span><span class="sh">'</span><span class="p">).</span>\
        <span class="nf">order_by</span><span class="p">(</span><span class="sh">'</span><span class="s">filesize</span><span class="sh">'</span><span class="p">).</span>\
        <span class="nf">desc</span><span class="p">().</span><span class="nf">first</span><span class="p">()</span>

<span class="n">audio_stream</span><span class="p">.</span><span class="nf">download</span><span class="p">()</span>
</code></pre></div></div>

<p>Только учтите, что формат аудио будет m4a, поэтому, если вам нужен другой, придётся воспользоваться <code class="language-plaintext highlighter-rouge">ffmpeg</code></p>

<h3 id="известные-проблемы">Известные проблемы</h3>

<h4 id="обход-dpi-не-работает">Обход DPI не работает</h4>

<p>Да, такое, конечно, возможно. Но у меня на провайдере ТТК всё работает отлично. Поэтому я могу лишь посоветовать использовать другие инструменты, например GoodbyeDPI.</p>

<h4 id="remote-end-closed-connection">Remote end closed connection</h4>

<p>А вот это действительно проблема, с которой я столкнулся. Она появилась, когда начали тормозить YouTube и я перешёл на nodpi. То ли ютуб банит подозрительную активность, то ли РКН химичит, а может в nodpi что-то не так. Я так и не понял. Если тут есть знатоки, может подскажут, что не так и как пофиксить)</p>]]></content><author><name></name></author><category term="habr" /><category term="youtube,python,dpi,скачивание" /><category term="видео,роскомнадзор,обход" /><category term="блокировок,обход" /><category term="блокировок" /><category term="youtube" /><summary type="html"><![CDATA[https://habr.com/ru/articles/870110/]]></summary></entry><entry><title type="html">Выборг</title><link href="http://localhost:4000/life/2024/05/07/viborg.html" rel="alternate" type="text/html" title="Выборг" /><published>2024-05-07T00:00:00+03:00</published><updated>2024-05-07T00:00:00+03:00</updated><id>http://localhost:4000/life/2024/05/07/viborg</id><content type="html" xml:base="http://localhost:4000/life/2024/05/07/viborg.html"><![CDATA[<p>Вы́борг[^Выборг] (фин. Viipuri, швед. Viborg, нем. Wiburg) — город в России, административный центр Выборгского муниципального района Ленинградской области. Образует Выборгское городское поселение (МО «Город Выборг») как единственный населённый пункт в его составе.</p>

<p>Город расположен на берегу Выборгского залива, находящегося в северо-восточной части Финского залива. Это крупный экономический, промышленный и культурный центр Ленинградской области, порт на Балтике, важный узел шоссейных и железных дорог.</p>

<p>Выборг, основанный в Средние века, с 2010 года является единственным историческим поселением на территории Ленинградской области. Среди достопримечательностей — Выборгский замок, парк Монрепо, библиотека Алвара Аалто. Всего в Выборге сосредоточено более трёхсот различных памятников: архитектурных, исторических, скульптурных, археологических, садово-паркового искусства. С марта 2010 года — «Город воинской славы».</p>

<h2 id="прогулка-по-городу">Прогулка по городу</h2>

<p><img src="/assets/images/life/viborg/IMG20240504124735.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504124739.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504124740.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504124957.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504125000.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504130020.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504125104.jpg" alt="Якоря Холла" />
Якоря Холла</p>

<h3 id="башня-ратуши">Башня Ратуши</h3>

<p>Башня Ратуши[^Башня Ратуши] — это одна из главных достопримечательностей старинного города Выборг. Она была построена в XIV веке и долгое время служила оборонительным сооружением.</p>

<p>В настоящее время в башне находится небольшой музей, который знакомит посетителей с историей и архитектурой этого здания. Кроме того, на верхнем этаже башни расположены панорамные окна, откуда открывается прекрасный вид на исторический центр города.</p>

<p><img src="/assets/images/life/viborg/IMG20240504130158.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504130203.jpg" alt="" /></p>

<h3 id="дом-купеческой-гильдии">Дом купеческой гильдии</h3>

<p>Дом купеческой гильдии Святого Духа[^Дом купеческой гильдии] — старинное здание в центре Выборга на углу Выборгской улицы (дом 8) и улицы Новой Заставы, средневековый памятник архитектуры.</p>

<p><img src="/assets/images/life/viborg/IMG20240504130633.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504130644.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504130818.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504142303.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504142320.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504142438.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504142623.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504143545.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504153212.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504153459.jpg" alt="" /></p>

<h3 id="кордегардия">Кордегардия</h3>

<p><img src="/assets/images/life/viborg/IMG20240504154057.jpg" alt="" /></p>

<h3 id="анненские-укрепления">Анненские укрепления</h3>

<p>Анненские укрепления[^Анненские укрепления] (Корон-Санкт-Анна) — бастионная крепость, редкий памятник русского оборонного зодчества второй четверти XVIII века. Самое значительное фортификационное сооружение XVIII века в Выборге. Также назывались Короно-Санкт-Анская крепость или Анненкрон (нем. Annenkrone — Корона Святой Анны) в честь императрицы Анны Иоанновны.</p>

<p><img src="/assets/images/life/viborg/IMG20240504154117.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154222.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154225.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154229.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154407.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154412.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154420.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504154432.jpg" alt="" /></p>

<h3 id="крепостная-улица">Крепостная улица</h3>
<p><img src="/assets/images/life/viborg/IMG20240505153037.jpg" alt="" /></p>

<h3 id="часовая-башня">Часовая башня</h3>
<p><img src="/assets/images/life/viborg/IMG20240505163839.jpg" alt="" /></p>

<h3 id="кафе-трамвай">Кафе-трамвай</h3>
<p><img src="/assets/images/life/viborg/IMG20240505152554.jpg" alt="" /></p>

<h3 id="самый-старый-жилой-дом-в-россии">Самый старый жилой дом в России</h3>

<p>Дом горожанина[^Дом горожанина] — Невысокий двухэтажный дом башенного типа с подвалом и толстыми стенами, сложенными из гранитных валунов, наряду со сходными усадьбой бюргера, домом купеческой гильдии Святого Духа и ещё одним гильдейским домом, входит в четвёрку маленьких частных «крепостей» XIV—XVII веков, средневековых бюргерских домов, которые на территории России можно увидеть только в Выборге.</p>

<p><img src="/assets/images/life/viborg/IMG20240505164709.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505164656.jpg" alt="" /></p>

<h3 id="красивые-развалины">Красивые развалины</h3>

<p><img src="/assets/images/life/viborg/IMG20240505165112.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505165119.jpg" alt="" /></p>

<h3 id="речной-порт">Речной порт</h3>

<p><img src="/assets/images/life/viborg/IMG20240505165737.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505165805.jpg" alt="" /></p>

<p><img src="/assets/images/life/viborg/IMG20240505170332.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240506085337.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240506085345.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504153452.jpg" alt="" /></p>

<h2 id="парк-монрепо">Парк Монрепо</h2>

<p>Монрепо́[^Монрепо] (фр. Mon Repos — «мой покой», «моё отдохновение») — скальный пейзажный парк на берегу бухты Защитной Выборгского залива, на острове Твердыш в северной части города Выборга в Ленинградской области. Официальное наименование — Государственный историко-архитектурный и природный музей-заповедник «Парк Монрепо». Находится в Петровском микрорайоне Выборга, рядом с Северным и Выборгским посёлками.</p>

<h3 id="главные-ворота">Главные ворота</h3>

<p>Въездные деревянные ворота в неоготическом стиле, украшенные стрельчатыми башнями и металлическим декором. Воссозданы в ходе реставрации.</p>

<p><img src="/assets/images/life/viborg/IMG20240504160906.jpg" alt="" /></p>

<h3 id="камень-китайского-зонтика">Камень китайского зонтика</h3>

<p>Восточный мудрец Вэнь Чженьхэн говорил: «Один валун должен явить красоту тысячи пиков». Такой красавец-валун есть и в парке Монрепо. К нему приведет тропинка, которая уходит вправо от входных ворот.[^Камень китайского зонтика]</p>

<p><img src="/assets/images/life/viborg/IMG20240504161219.jpg" alt="" /></p>

<h3 id="обелиск-братьям-броглио">Обелиск братьям Броглио</h3>

<p><img src="/assets/images/life/viborg/IMG20240504162347.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504162423.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504162500.jpg" alt="" /></p>

<h3 id="погреб-ледник">Погреб-ледник</h3>

<p><img src="/assets/images/life/viborg/IMG20240504165641.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504170609.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504170611.jpg" alt="" /></p>

<h3 id="дом-садовника">Дом садовника</h3>

<p><img src="/assets/images/life/viborg/IMG20240504165452.jpg" alt="" /></p>

<h3 id="капелла-людвигсбург-на-острове-людвигштайн">Капелла Людвигсбург на острове Людвигштайн»</h3>

<p><img src="/assets/images/life/viborg/IMG20240504171234.jpg" alt="" /></p>

<h3 id="хижина-отшельника">Хижина отшельника</h3>

<p><img src="/assets/images/life/viborg/IMG20240504172311.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504173811.jpg" alt="" /></p>

<h3 id="грот-желаний">Грот Желаний</h3>

<p><img src="/assets/images/life/viborg/IMG20240504172654.jpg" alt="" /></p>

<h3 id="край-света">Край света</h3>

<p><img src="/assets/images/life/viborg/IMG20240504173241.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504173200.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504172840.jpg" alt="" /></p>

<h3 id="храм-нептуна">Храм Нептуна</h3>

<p><img src="/assets/images/life/viborg/IMG20240504174636.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504161002.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504161830.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504162053.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504162059.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504162210.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504172527.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504172758.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504164147.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504164152.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504162253.jpg" alt="" /></p>

<h2 id="выборгский-замок">Выборгский замок</h2>

<p>Выборгский замок[^Выборгский замок] был основан в 1293 году во время третьего шведского крестового похода. Морское ополчение высадилось на побережье Финского залива в районе нынешнего Выборга и разорило поселение карел и карельский сторожевой пункт на небольшом острове. Шведы заложили на острове замок и назвали его Выборгом (в переводе со старошведского — «Святая крепость»).</p>

<p>О первоначальном облике замка достоверных сведений не сохранилось. Археологические исследования показали, что вокруг центральной возвышенной части острова (современный Кузнечный двор) была сооружена каменная стена. А в центре острова — возведена четырехугольная каменная башня-донжон. Шведы назвали ее башней Святого Олафа в честь короля Олафа II Харальдссона, утвердившего христианство в Норвегии.</p>

<p><img src="/assets/images/life/viborg/IMG20240504153050.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504153217.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504153321.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240504184458_BURST000_COVER.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505114104.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505112017.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113725.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113735.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113743.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113957.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505114057.jpg" alt="" /></p>

<h3 id="музеи-замка">Музеи замка</h3>

<p>К столетию со дня рождения Ленина, в апреле 1970 года, в Выборгском замке открылась первая постоянная экспозиция — «Историческое и революционное прошлое края». Чуть позже появилась самая старая из ныне действующих выставок — экспозиция «Карельский перешеек в годы Великой Отечественной войны»[^История музея]</p>

<p><img src="/assets/images/life/viborg/IMG20240505115615.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505120453.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505120616.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505120752.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505121334.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505111353.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505111751.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505111858.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505112348.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505112609.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505112830.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505112947.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113115.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113244.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505113350.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505115454.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505115459.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505115521.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505115157.jpg" alt="" /></p>

<h2 id="сваргас">Сваргас</h2>

<p>Средневековый город Cваргас - столица варяжских земель[^Сваргас]</p>

<p>Историко - этнографический музей под открытым небом, воссозданный в натуральную величину, как крепость балтийских славян – венедов периода раннего средневековья VIII - XII в.в., в период зарождения Российской государственности. “Сваргас” — индоевропейское слово, переводится как «Солнечное небо». (Местоположение: 60.82799 28.83713)</p>

<p><img src="/assets/images/life/viborg/IMG20240505125833.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505125432.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505130401.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505130917.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505130951.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505131856.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505131922.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505132102.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505132626.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505133508.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505133509.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505133511.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505133608.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505133732.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505133804.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505134116.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505134203.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505135430_03.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505140418.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505140619.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505142200.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505142201.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505144442.jpg" alt="" />
<img src="/assets/images/life/viborg/IMG20240505134056.jpg" alt="" /></p>]]></content><author><name></name></author><category term="life" /><summary type="html"><![CDATA[Вы́борг[^Выборг] (фин. Viipuri, швед. Viborg, нем. Wiburg) — город в России, административный центр Выборгского муниципального района Ленинградской области. Образует Выборгское городское поселение (МО «Город Выборг») как единственный населённый пункт в его составе.]]></summary></entry><entry><title type="html">Великий Новгород</title><link href="http://localhost:4000/life/2023/09/01/velikiy-novgorod.html" rel="alternate" type="text/html" title="Великий Новгород" /><published>2023-09-01T00:00:00+03:00</published><updated>2023-09-01T00:00:00+03:00</updated><id>http://localhost:4000/life/2023/09/01/velikiy-novgorod</id><content type="html" xml:base="http://localhost:4000/life/2023/09/01/velikiy-novgorod.html"><![CDATA[<h2 id="отель">Отель</h2>

<p>Наш отель находится в частном секторе, который, в свою очередь, обосновался рядом с центром города. Отель наш не единственный, люди просто строят 3-х этажные дома у себя на участке, делят их на небольшие комнатки и сдают туристам. Наш был в стиле средневековья.</p>

<p><img src="/assets/images/life/velikiy-novgorod/hotel/photo_2023-09-01%2018.25.32.jpeg" alt="" />
Хозяин отеля любит замки</p>

<p><img src="/assets/images/life/velikiy-novgorod/hotel/IMG20230826113419.jpg" alt="" />
Внутри есть дворик со статуями драконов. К прудику подходить нельзя 🙁</p>

<p><img src="/assets/images/life/velikiy-novgorod/hotel/image%20%286%29.jpg" alt="" />
Меня заставили скосплеить рыцаря, но результат предсказуем</p>

<p><img src="/assets/images/life/velikiy-novgorod/hotel/IMG20230825151208%7E2.jpg" alt="" />
Главная фишка отеля – крытый бассейн на территории (немного тесный, вода невкусная, но в жару купаться – кайф)</p>

<h2 id="белая-башня">Белая башня</h2>

<p>Одно из оборонных сооружений города - четырехэтажная башня, раньше была частью внешней стены.</p>

<p><img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826120100.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826120224.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826120512.jpg" alt="" />
Тут видно, почему башня называется Белая</p>

<p><img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826121244.jpg" alt="" />
В древности люди были настолько суровы, что голыми руками заряжали огромные каменные ядра в маленькие чугунные пушки</p>

<p><img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826121734.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826121921.jpg" alt="" />
Лестница крутая, не представляю как по ней можно было бежать, да еще и с грузом</p>

<p><img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826122015.jpg" alt="" />
Под стеклом можно увидеть технологию стройки древних</p>

<p><img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826122214.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826122225.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826122550.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826122557.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/IMG20230826122739.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/photo_2023-09-01%2018.25.25.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/photo_2023-09-01%2018.25.27.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/photo_2023-09-01%2018.25.28.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/photo_2023-09-01%2018.25.29.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/belaya-bashnya/photo_2023-09-01%2018.25.30.jpeg" alt="" /></p>

<h2 id="детинец">Детинец</h2>

<p>Центр города<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup> защищен стенами, являлся самым безопаным местом. Экскурсовод утверждала, что это слово обозначает - мать, но википедия с этим не совсем <a href="https://ru.wikipedia.org/wiki/%D0%94%D0%B5%D1%82%D0%B8%D0%BD%D0%B5%D1%86">согласна</a></p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230825184223_01.jpg" alt="" />
Мы стоим не месте внешней стены, на месте которой сейчас находится прогулосная дорожка.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230826204448.jpg" alt="" /></p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230826132003.jpg" alt="" />
У каждой башни есть имя и ТТХ</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.10.jpeg" alt="" />
Эта башня - сквозная, через нее проходили в центр города. Или не проходили, тут уж как стрелки решат.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230825185139.jpg" alt="" />
Памятник <a href="https://1000russia.ru/">Тысячелетие России</a></p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230825185258.jpg" alt="" />
Если разобраться, кто и почему на нем запечатлен, можно сдать ЕГЭ по истории России.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230825185509.jpg" alt="" />
Скульпторы Михаил Микешин и Иван Шредер заслужили всемирную известность благодаря этому памятнику.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230825190448.jpg" alt="" />
Ограда отлита в наше время в Санкт-Петербурге</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230827125631.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230826133725.jpg" alt="" />
<a href="https://www.visitnovgorod.ru/sights/hanseatic_fountain.html">Ганзейский фонтан</a>.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230826133733.jpg" alt="" />
В Средние века Новгород активно торговал с Ганзейским союзом, история которого насчитывает пять столетий. Это естественные связи обусловленные географией и они периодически возрождаются.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230827133351.jpg" alt="" />
Архитектурным центром Кремля со времени его основания является <a href="http://sofia-novgorod.cerkov.ru/main-page/">Софийский собор</a>. София — древнейший православный кафедральный храм на территории Российской Федерации, выдающееся произведение русской архитектуры домонгольского периода. <sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup></p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/IMG20230827133508.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/image%20%281%29.jpg" alt="" />
Первый колокол на Руси (слева)</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/image%20%282%29.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.00.jpeg" alt="" />
Главный — западный — портал храма украшен древними бронзовыми вратами.. Их выполнили в Магдебурге в середине XII в. немецкие мастера. В клеймах запечатлены сюжеты Ветхого и Нового Заветов, сцены символического характера и исторические лица: заказчик  врат Плоцкий архиепископ Александр, мастера-литейщики Риквин и Вайсмут. Вероятно, изначально врата предназначались для собора в Плоцке; Новгород же приобрел это  замечательное произведение западноевропейского бронзового литья, по-видимому, уже в первой половине XV в.. Местный мастер смонтировал створки врат, дополнив “заморское” изделие новыми рельефами и русскими переводами к латинским надписям. В нижней части левой створы он поместил “автопортрет” с атрибутами ремесла, подписав его кириллицей: “Аврам”</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.02.jpeg" alt="" />
По правилам – один купол должен быть золотым. Максимальное количество золотых куполов не уточняется, так что сейчас по-русски, с размахом, золотим все.</p>

<p><img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.12.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.15.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.24.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/photo_2023-09-01%2018.25.44.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/detinec/telegram-cloud-document-2-5424621489590646786.mp4" alt="" /></p>

<h2 id="прогулка">Прогулка</h2>

<p>Нам очень повезло, по чистой случайности мы попали на день города. Великому Новгороду исполнилось 1164 года! Как-то с трудом принимаются такие цифры.</p>

<p><img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230825172710.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230825175025%7E4.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230825191151.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230825191501.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230826135119.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230826135136%7E2.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230826144514.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230826144525.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230826155109.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230826200941.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/IMG20230827142423.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/image%20%283%29.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/image%20%284%29.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/image%20%288%29.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/image.jpg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.24.57.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.03.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.18.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.21.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.34.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.35.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.37.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.38.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/photo_2023-09-01%2018.25.40.jpeg" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/telegram-cloud-document-2-5435954976276885829.mp4" alt="" />
<img src="/assets/images/life/velikiy-novgorod/progulka/telegram-cloud-document-2-5435954976276885830.mp4" alt="" /></p>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>Интеренстый факт: городом называли сами защитные стены. Поселение, не окруженное стенами, городом не являлось. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2">
      <p>Источник - <a href="http://sofia-novgorod.cerkov.ru/main-page/">http://sofia-novgorod.cerkov.ru/main-page/</a> <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="life" /><summary type="html"><![CDATA[поездка в г. Великий Новгород, 25 августа 2023]]></summary></entry><entry><title type="html">Дом Cовы на р.Тихой</title><link href="http://localhost:4000/life/2023/06/14/dom-sovi.html" rel="alternate" type="text/html" title="Дом Cовы на р.Тихой" /><published>2023-06-14T00:00:00+03:00</published><updated>2023-06-14T00:00:00+03:00</updated><id>http://localhost:4000/life/2023/06/14/dom-sovi</id><content type="html" xml:base="http://localhost:4000/life/2023/06/14/dom-sovi.html"><![CDATA[<p>Дорога из “Терминатора”
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.01.jpeg" alt="" /></p>

<p>Наш домик во всей красе!
<img src="/assets/images/life/priozersk/photo_2023-06-14 13.36.49.jpeg" alt="" /></p>

<p>Наш домик сзади
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.31.27.jpeg" alt="" /></p>

<p>Вид на участок с острова
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.31.18.jpeg" alt="" /></p>

<p>Огромные кусты сирени в цвету
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.31.44.jpeg" alt="" /></p>

<p>Сети на участке нет, мы нашли место, где хоть как-то ловит и скачиваем тиктоки
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.31.15.jpeg" alt="" /></p>

<p>Не знаю что это за растение, но их тут много и растут они вот так, парами. А может это один цветок?
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.31.49.jpeg" alt="" /></p>

<p>Мы возвращаемся после зарядки с островка
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.33.12.jpeg" alt="" /></p>

<p>Таким кустарником усеяно все в лесу, выглядит как ухоженная клумба
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.08.jpeg" alt="" /></p>

<p>Лесная дорожка
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.13.jpeg" alt="" /></p>

<p>Никогда не жарьте сосиски на костре, не получится :)
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.20.jpeg" alt="" /></p>

<p>Озеро незаметно начинается через 3 .. 2 ..
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.27.jpeg" alt="" /></p>

<p>Вообще лес тут дремучий, но вот мы нашли полянку
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.33.jpeg" alt="" /></p>

<p>Чья-то одинокая будка
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.36.jpeg" alt="" /></p>

<p>Валун и мы для масштаба
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.40.jpeg" alt="" /></p>

<p>Река Тихая никакая не тихая
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.45.jpeg" alt="" /></p>

<p>Все вокруг цветет
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.52.jpeg" alt="" /></p>

<p>Спокойная часть реки Тихой
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.32.59.jpeg" alt="" /></p>

<p>Обратно идем пешком через лес
<img src="/assets/images/life/priozersk/photo_2023-06-14 12.33.20.jpeg" alt="" /></p>]]></content><author><name></name></author><category term="life" /><summary type="html"><![CDATA[г. Приозерск, Дом Cовы на р.Тихой]]></summary></entry><entry><title type="html">Markdown: Syntax</title><link href="http://localhost:4000/other/2023/05/12/markdown-cheat-sheet.html" rel="alternate" type="text/html" title="Markdown: Syntax" /><published>2023-05-12T00:00:00+03:00</published><updated>2023-05-12T00:00:00+03:00</updated><id>http://localhost:4000/other/2023/05/12/markdown-cheat-sheet</id><content type="html" xml:base="http://localhost:4000/other/2023/05/12/markdown-cheat-sheet.html"><![CDATA[<p><a href="https://daringfireball.net/projects/markdown/syntax">Original page</a></p>

<h2 id="overview">Overview</h2>

<h3 id="philosophy">Philosophy</h3>

<p>Markdown is intended to be as easy-to-read and easy-to-write as is feasible.</p>

<p>Readability, however, is emphasized above all else. A Markdown-formatted
document should be publishable as-is, as plain text, without looking
like it’s been marked up with tags or formatting instructions. While
Markdown’s syntax has been influenced by several existing text-to-<abbr title="Hyper Text Markup Language">HTML</abbr>
filters – including <a href="http://docutils.sourceforge.net/mirror/setext.html">Setext</a>, <a href="http://www.aaronsw.com/2002/atx/">atx</a>, <a href="https://web.archive.org/web/20021226035527/http://textism.com/tools/textile/">Textile</a>, <a href="http://docutils.sourceforge.net/rst.html">reStructuredText</a>,
<a href="http://www.triptico.com/software/grutatxt.html">Grutatext</a>, and <a href="http://ettext.taint.org/doc/">EtText</a> – the single biggest source of
inspiration for Markdown’s syntax is the format of plain text email.</p>

<p>To this end, Markdown’s syntax is comprised entirely of punctuation
characters, which punctuation characters have been carefully chosen so
as to look like what they mean. E.g., asterisks around a word actually
look like *emphasis*. Markdown lists look like, well, lists. Even
blockquotes look like quoted passages of text, assuming you’ve ever
used email.</p>

<h3 id="inline-html">Inline <abbr title="Hyper Text Markup Language">HTML</abbr></h3>

<p>Markdown’s syntax is intended for one purpose: to be used as a
format for <em>writing</em> for the web.</p>

<p>Markdown is not a replacement for <abbr title="Hyper Text Markup Language">HTML</abbr>, or even close to it. Its
syntax is very small, corresponding only to a very small subset of
<abbr title="Hyper Text Markup Language">HTML</abbr> tags. The idea is <em>not</em> to create a syntax that makes it easier
to insert <abbr title="Hyper Text Markup Language">HTML</abbr> tags. In my opinion, <abbr title="Hyper Text Markup Language">HTML</abbr> tags are already easy to
insert. The idea for Markdown is to make it easy to read, write, and
edit prose. <abbr title="Hyper Text Markup Language">HTML</abbr> is a <em>publishing</em> format; Markdown is a <em>writing</em>
format. Thus, Markdown’s formatting syntax only addresses issues that
can be conveyed in plain text.</p>

<p>For any markup that is not covered by Markdown’s syntax, you simply
use <abbr title="Hyper Text Markup Language">HTML</abbr> itself. There’s no need to preface it or delimit it to
indicate that you’re switching from Markdown to <abbr title="Hyper Text Markup Language">HTML</abbr>; you just use
the tags.</p>

<p>The only restrictions are that block-level <abbr title="Hyper Text Markup Language">HTML</abbr> elements – e.g. <code class="language-plaintext highlighter-rouge">&lt;div&gt;</code>,
<code class="language-plaintext highlighter-rouge">&lt;table&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;pre&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;p&gt;</code>, etc. – must be separated from surrounding
content by blank lines, and the start and end tags of the block should
not be indented with tabs or spaces. Markdown is smart enough not
to add extra (unwanted) <code class="language-plaintext highlighter-rouge">&lt;p&gt;</code> tags around <abbr title="Hyper Text Markup Language">HTML</abbr> block-level tags.</p>

<p>For example, to add an <abbr title="Hyper Text Markup Language">HTML</abbr> table to a Markdown article:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is a regular paragraph.

&lt;table&gt;
    &lt;tr&gt;
        &lt;td&gt;Foo&lt;/td&gt;
    &lt;/tr&gt;
&lt;/table&gt;

This is another regular paragraph.
</code></pre></div></div>

<p>Note that Markdown formatting syntax is not processed within block-level
<abbr title="Hyper Text Markup Language">HTML</abbr> tags. E.g., you can’t use Markdown-style <code class="language-plaintext highlighter-rouge">*emphasis*</code> inside an
<abbr title="Hyper Text Markup Language">HTML</abbr> block.</p>

<p>Span-level <abbr title="Hyper Text Markup Language">HTML</abbr> tags – e.g. <code class="language-plaintext highlighter-rouge">&lt;span&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;cite&gt;</code>, or <code class="language-plaintext highlighter-rouge">&lt;del&gt;</code> – can be
used anywhere in a Markdown paragraph, list item, or header. If you
want, you can even use <abbr title="Hyper Text Markup Language">HTML</abbr> tags instead of Markdown formatting; e.g. if
you’d prefer to use <abbr title="Hyper Text Markup Language">HTML</abbr> <code class="language-plaintext highlighter-rouge">&lt;a&gt;</code> or <code class="language-plaintext highlighter-rouge">&lt;img&gt;</code> tags instead of Markdown’s
link or image syntax, go right ahead.</p>

<p>Unlike block-level <abbr title="Hyper Text Markup Language">HTML</abbr> tags, Markdown syntax <em>is</em> processed within
span-level tags.</p>

<h3 id="automatic-escaping-for-special-characters">Automatic Escaping for Special Characters</h3>

<p>In <abbr title="Hyper Text Markup Language">HTML</abbr>, there are two characters that demand special treatment: <code class="language-plaintext highlighter-rouge">&lt;</code>
and <code class="language-plaintext highlighter-rouge">&amp;</code>. Left angle brackets are used to start tags; ampersands are
used to denote <abbr title="Hyper Text Markup Language">HTML</abbr> entities. If you want to use them as literal
characters, you must escape them as entities, e.g. <code class="language-plaintext highlighter-rouge">&amp;lt;</code>, and
<code class="language-plaintext highlighter-rouge">&amp;amp;</code>.</p>

<p>Ampersands in particular are bedeviling for web writers. If you want to
write about ‘AT&amp;T’, you need to write ‘<code class="language-plaintext highlighter-rouge">AT&amp;amp;T</code>’. You even need to
escape ampersands within URLs. Thus, if you want to link to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://images.google.com/images?num=30&amp;q=larry+bird
</code></pre></div></div>

<p>you need to encode the URL as:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://images.google.com/images?num=30&amp;amp;q=larry+bird
</code></pre></div></div>

<p>in your anchor tag <code class="language-plaintext highlighter-rouge">href</code> attribute. Needless to say, this is easy to
forget, and is probably the single most common source of <abbr title="Hyper Text Markup Language">HTML</abbr> validation
errors in otherwise well-marked-up web sites.</p>

<p>Markdown allows you to use these characters naturally, taking care of
all the necessary escaping for you. If you use an ampersand as part of
an <abbr title="Hyper Text Markup Language">HTML</abbr> entity, it remains unchanged; otherwise it will be translated
into <code class="language-plaintext highlighter-rouge">&amp;amp;</code>.</p>

<p>So, if you want to include a copyright symbol in your article, you can write:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&amp;copy;
</code></pre></div></div>

<p>and Markdown will leave it alone. But if you write:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AT&amp;T
</code></pre></div></div>

<p>Markdown will translate it to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AT&amp;amp;T
</code></pre></div></div>

<p>Similarly, because Markdown supports <a href="#html">inline <abbr title="Hyper Text Markup Language">HTML</abbr></a>, if you use
angle brackets as delimiters for <abbr title="Hyper Text Markup Language">HTML</abbr> tags, Markdown will treat them as
such. But if you write:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>4 &lt; 5
</code></pre></div></div>

<p>Markdown will translate it to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>4 &amp;lt; 5
</code></pre></div></div>

<p>However, inside Markdown code spans and blocks, angle brackets and
ampersands are <em>always</em> encoded automatically. This makes it easy to use
Markdown to write about <abbr title="Hyper Text Markup Language">HTML</abbr> code. (As opposed to raw <abbr title="Hyper Text Markup Language">HTML</abbr>, which is a
terrible format for writing about <abbr title="Hyper Text Markup Language">HTML</abbr> syntax, because every single <code class="language-plaintext highlighter-rouge">&lt;</code>
and <code class="language-plaintext highlighter-rouge">&amp;</code> in your example code needs to be escaped.)</p>

<hr />

<h2 id="block-elements">Block Elements</h2>

<h3 id="paragraphs-and-line-breaks">Paragraphs and Line Breaks</h3>

<p>A paragraph is simply one or more consecutive lines of text, separated
by one or more blank lines. (A blank line is any line that looks like a
blank line – a line containing nothing but spaces or tabs is considered
blank.) Normal paragraphs should not be indented with spaces or tabs.</p>

<p>The implication of the “one or more consecutive lines of text” rule is
that Markdown supports “hard-wrapped” text paragraphs. This differs
significantly from most other text-to-<abbr title="Hyper Text Markup Language">HTML</abbr> formatters (including Movable
Type’s “Convert Line Breaks” option) which translate every line break
character in a paragraph into a <code class="language-plaintext highlighter-rouge">&lt;br /&gt;</code> tag.</p>

<p>When you <em>do</em> want to insert a <code class="language-plaintext highlighter-rouge">&lt;br /&gt;</code> break tag using Markdown, you
end a line with two or more spaces, then type return.</p>

<p>Yes, this takes a tad more effort to create a <code class="language-plaintext highlighter-rouge">&lt;br /&gt;</code>, but a simplistic
“every line break is a <code class="language-plaintext highlighter-rouge">&lt;br /&gt;</code>” rule wouldn’t work for Markdown.
Markdown’s email-style <a href="#blockquote">blockquoting</a> and multi-paragraph <a href="#list">list items</a>
work best – and look better – when you format them with hard breaks.</p>

<h3 id="headers">Headers</h3>

<p>Markdown supports two styles of headers, <a href="http://docutils.sourceforge.net/mirror/setext.html">Setext</a> and <a href="http://www.aaronsw.com/2002/atx/">atx</a>.</p>

<p>Setext-style headers are “underlined” using equal signs (for first-level
headers) and dashes (for second-level headers). For example:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is an H1
=============

This is an H2
-------------
</code></pre></div></div>

<p>Any number of underlining <code class="language-plaintext highlighter-rouge">=</code>’s or <code class="language-plaintext highlighter-rouge">-</code>’s will work.</p>

<p>Atx-style headers use 1-6 hash characters at the start of the line,
corresponding to header levels 1-6. For example:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># This is an H1

## This is an H2

###### This is an H6
</code></pre></div></div>

<p>Optionally, you may “close” atx-style headers. This is purely
cosmetic – you can use this if you think it looks better. The
closing hashes don’t even need to match the number of hashes
used to open the header. (The number of opening hashes
determines the header level.) :</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># This is an H1 #

## This is an H2 ##

### This is an H3 ######
</code></pre></div></div>

<h3 id="blockquotes">Blockquotes</h3>

<p>Markdown uses email-style <code class="language-plaintext highlighter-rouge">&gt;</code> characters for blockquoting. If you’re
familiar with quoting passages of text in an email message, then you
know how to create a blockquote in Markdown. It looks best if you hard
wrap the text and put a <code class="language-plaintext highlighter-rouge">&gt;</code> before every line:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,
&gt; consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.
&gt; Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.
&gt;
&gt; Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse
&gt; id sem consectetuer libero luctus adipiscing.
</code></pre></div></div>

<p>Markdown allows you to be lazy and only put the <code class="language-plaintext highlighter-rouge">&gt;</code> before the first
line of a hard-wrapped paragraph:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,
consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.
Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.

&gt; Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse
id sem consectetuer libero luctus adipiscing.
</code></pre></div></div>

<p>Blockquotes can be nested (i.e. a blockquote-in-a-blockquote) by
adding additional levels of <code class="language-plaintext highlighter-rouge">&gt;</code>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; This is the first level of quoting.
&gt;
&gt; &gt; This is nested blockquote.
&gt;
&gt; Back to the first level.
</code></pre></div></div>

<p>Blockquotes can contain other Markdown elements, including headers, lists,
and code blocks:</p>

<blockquote>
  <h2 id="this-is-a-header">This is a header.</h2>

  <ol>
    <li>This is the first list item.</li>
    <li>This is the second list item.</li>
  </ol>

  <p>Here’s some example code:</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return shell_exec("echo $input | $markdown_script");
</code></pre></div>  </div>
</blockquote>

<p>Any decent text editor should make email-style quoting easy. For
example, with BBEdit, you can make a selection and choose Increase
Quote Level from the Text menu.</p>

<h3 id="lists">Lists</h3>

<p>Markdown supports ordered (numbered) and unordered (bulleted) lists.</p>

<p>Unordered lists use asterisks, pluses, and hyphens – interchangably
– as list markers:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   Red
*   Green
*   Blue
</code></pre></div></div>

<p>is equivalent to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+   Red
+   Green
+   Blue
</code></pre></div></div>

<p>and:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>-   Red
-   Green
-   Blue
</code></pre></div></div>

<p>Ordered lists use numbers followed by periods:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1.  Bird
2.  McHale
3.  Parish
</code></pre></div></div>

<p>It’s important to note that the actual numbers you use to mark the
list have no effect on the <abbr title="Hyper Text Markup Language">HTML</abbr> output Markdown produces. The <abbr title="Hyper Text Markup Language">HTML</abbr>
Markdown produces from the above list is:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;ol&gt;
&lt;li&gt;Bird&lt;/li&gt;
&lt;li&gt;McHale&lt;/li&gt;
&lt;li&gt;Parish&lt;/li&gt;
&lt;/ol&gt;
</code></pre></div></div>

<p>If you instead wrote the list in Markdown like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1.  Bird
1.  McHale
1.  Parish
</code></pre></div></div>

<p>or even:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>3. Bird
1. McHale
8. Parish
</code></pre></div></div>

<p>you’d get the exact same <abbr title="Hyper Text Markup Language">HTML</abbr> output. The point is, if you want to,
you can use ordinal numbers in your ordered Markdown lists, so that
the numbers in your source match the numbers in your published <abbr title="Hyper Text Markup Language">HTML</abbr>.
But if you want to be lazy, you don’t have to.</p>

<p>If you do use lazy list numbering, however, you should still start the
list with the number 1. At some point in the future, Markdown may support
starting ordered lists at an arbitrary number.</p>

<p>List markers typically start at the left margin, but may be indented by
up to three spaces. List markers must be followed by one or more spaces
or a tab.</p>

<p>To make lists look nice, you can wrap items with hanging indents:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   Lorem ipsum dolor sit amet, consectetuer adipiscing elit.
    Aliquam hendrerit mi posuere lectus. Vestibulum enim wisi,
    viverra nec, fringilla in, laoreet vitae, risus.
*   Donec sit amet nisl. Aliquam semper ipsum sit amet velit.
    Suspendisse id sem consectetuer libero luctus adipiscing.
</code></pre></div></div>

<p>But if you want to be lazy, you don’t have to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   Lorem ipsum dolor sit amet, consectetuer adipiscing elit.
Aliquam hendrerit mi posuere lectus. Vestibulum enim wisi,
viverra nec, fringilla in, laoreet vitae, risus.
*   Donec sit amet nisl. Aliquam semper ipsum sit amet velit.
Suspendisse id sem consectetuer libero luctus adipiscing.
</code></pre></div></div>

<p>If list items are separated by blank lines, Markdown will wrap the
items in <code class="language-plaintext highlighter-rouge">&lt;p&gt;</code> tags in the <abbr title="Hyper Text Markup Language">HTML</abbr> output. For example, this input:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   Bird
*   Magic
</code></pre></div></div>

<p>will turn into:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;ul&gt;
&lt;li&gt;Bird&lt;/li&gt;
&lt;li&gt;Magic&lt;/li&gt;
&lt;/ul&gt;
</code></pre></div></div>

<p>But this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   Bird

*   Magic
</code></pre></div></div>

<p>will turn into:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;ul&gt;
&lt;li&gt;&lt;p&gt;Bird&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;Magic&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;
</code></pre></div></div>

<p>List items may consist of multiple paragraphs. Each subsequent
paragraph in a list item must be indented by either 4 spaces
or one tab:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1.  This is a list item with two paragraphs. Lorem ipsum dolor
    sit amet, consectetuer adipiscing elit. Aliquam hendrerit
    mi posuere lectus.

    Vestibulum enim wisi, viverra nec, fringilla in, laoreet
    vitae, risus. Donec sit amet nisl. Aliquam semper ipsum
    sit amet velit.

2.  Suspendisse id sem consectetuer libero luctus adipiscing.
</code></pre></div></div>

<p>It looks nice if you indent every line of the subsequent
paragraphs, but here again, Markdown will allow you to be
lazy:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   This is a list item with two paragraphs.

    This is the second paragraph in the list item. You're
only required to indent the first line. Lorem ipsum dolor
sit amet, consectetuer adipiscing elit.

*   Another item in the same list.
</code></pre></div></div>

<p>To put a blockquote within a list item, the blockquote’s <code class="language-plaintext highlighter-rouge">&gt;</code>
delimiters need to be indented:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   A list item with a blockquote:

    &gt; This is a blockquote
    &gt; inside a list item.
</code></pre></div></div>

<p>To put a code block within a list item, the code block needs
to be indented <em>twice</em> – 8 spaces or two tabs:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*   A list item with a code block:

        &lt;code goes here&gt;
</code></pre></div></div>

<p>It’s worth noting that it’s possible to trigger an ordered list by
accident, by writing something like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1986. What a great season.
</code></pre></div></div>

<p>In other words, a <em>number-period-space</em> sequence at the beginning of a
line. To avoid this, you can backslash-escape the period:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1986\. What a great season.
</code></pre></div></div>

<h3 id="code-blocks">Code Blocks</h3>

<p>Pre-formatted code blocks are used for writing about programming or
markup source code. Rather than forming normal paragraphs, the lines
of a code block are interpreted literally. Markdown wraps a code block
in both <code class="language-plaintext highlighter-rouge">&lt;pre&gt;</code> and <code class="language-plaintext highlighter-rouge">&lt;code&gt;</code> tags.</p>

<p>To produce a code block in Markdown, simply indent every line of the
block by at least 4 spaces or 1 tab. For example, given this input:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is a normal paragraph:

    This is a code block.
</code></pre></div></div>

<p>Markdown will generate:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;This is a normal paragraph:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;This is a code block.
&lt;/code&gt;&lt;/pre&gt;
</code></pre></div></div>

<p>One level of indentation – 4 spaces or 1 tab – is removed from each
line of the code block. For example, this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Here is an example of AppleScript:

    tell application "Foo"
        beep
    end tell
</code></pre></div></div>

<p>will turn into:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;Here is an example of AppleScript:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;tell application "Foo"
    beep
end tell
&lt;/code&gt;&lt;/pre&gt;
</code></pre></div></div>

<p>A code block continues until it reaches a line that is not indented
(or the end of the article).</p>

<p>Within a code block, ampersands (<code class="language-plaintext highlighter-rouge">&amp;</code>) and angle brackets (<code class="language-plaintext highlighter-rouge">&lt;</code> and <code class="language-plaintext highlighter-rouge">&gt;</code>)
are automatically converted into <abbr title="Hyper Text Markup Language">HTML</abbr> entities. This makes it very
easy to include example <abbr title="Hyper Text Markup Language">HTML</abbr> source code using Markdown – just paste
it and indent it, and Markdown will handle the hassle of encoding the
ampersands and angle brackets. For example, this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    &lt;div class="footer"&gt;
        &amp;copy; 2004 Foo Corporation
    &lt;/div&gt;
</code></pre></div></div>

<p>will turn into:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;pre&gt;&lt;code&gt;&amp;lt;div class="footer"&amp;gt;
    &amp;amp;copy; 2004 Foo Corporation
&amp;lt;/div&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
</code></pre></div></div>

<p>Regular Markdown syntax is not processed within code blocks. E.g.,
asterisks are just literal asterisks within a code block. This means
it’s also easy to use Markdown to write about Markdown’s own syntax.</p>

<h3 id="horizontal-rules">Horizontal Rules</h3>

<p>You can produce a horizontal rule tag (<code class="language-plaintext highlighter-rouge">&lt;hr /&gt;</code>) by placing three or
more hyphens, asterisks, or underscores on a line by themselves. If you
wish, you may use spaces between the hyphens or asterisks. Each of the
following lines will produce a horizontal rule:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>* * *

***

*****

- - -

---------------------------------------
</code></pre></div></div>

<hr />

<h2 id="span-elements">Span Elements</h2>

<h3 id="links">Links</h3>

<p>Markdown supports two style of links: <em>inline</em> and <em>reference</em>.</p>

<p>In both styles, the link text is delimited by [square brackets].</p>

<p>To create an inline link, use a set of regular parentheses immediately
after the link text’s closing square bracket. Inside the parentheses,
put the URL where you want the link to point, along with an <em>optional</em>
title for the link, surrounded in quotes. For example:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is [an example](http://example.com/ "Title") inline link.

[This link](http://example.net/) has no title attribute.
</code></pre></div></div>

<p>Will produce:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;This is &lt;a href="http://example.com/" title="Title"&gt;
an example&lt;/a&gt; inline link.&lt;/p&gt;

&lt;p&gt;&lt;a href="http://example.net/"&gt;This link&lt;/a&gt; has no
title attribute.&lt;/p&gt;
</code></pre></div></div>

<p>If you’re referring to a local resource on the same server, you can
use relative paths:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>See my [About](/about/) page for details.
</code></pre></div></div>

<p>Reference-style links use a second set of square brackets, inside
which you place a label of your choosing to identify the link:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is [an example][id] reference-style link.
</code></pre></div></div>

<p>You can optionally use a space to separate the sets of brackets:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is [an example] [id] reference-style link.
</code></pre></div></div>

<p>Then, anywhere in the document, you define your link label like this,
on a line by itself:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[id]: http://example.com/  "Optional Title Here"
</code></pre></div></div>

<p>That is:</p>

<ul>
  <li>Square brackets containing the link identifier (optionally
indented from the left margin using up to three spaces);</li>
  <li>followed by a colon;</li>
  <li>followed by one or more spaces (or tabs);</li>
  <li>followed by the URL for the link;</li>
  <li>optionally followed by a title attribute for the link, enclosed
in double or single quotes, or enclosed in parentheses.</li>
</ul>

<p>The following three link definitions are equivalent:</p>

<p><strong>Note:</strong> There is a known bug in Markdown.pl 1.0.1 which prevents
single quotes from being used to delimit link titles.</p>

<p>The link URL may, optionally, be surrounded by angle brackets:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[id]: &lt;http://example.com/&gt;  "Optional Title Here"
</code></pre></div></div>

<p>You can put the title attribute on the next line and use extra spaces
or tabs for padding, which tends to look better with longer URLs:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[id]: http://example.com/longish/path/to/resource/here
    "Optional Title Here"
</code></pre></div></div>

<p>Link definitions are only used for creating links during Markdown
processing, and are stripped from your document in the <abbr title="Hyper Text Markup Language">HTML</abbr> output.</p>

<p>Link definition names may consist of letters, numbers, spaces, and
punctuation – but they are <em>not</em> case sensitive. E.g. these two
links:</p>

<p>[link text][a]
  [link text][A]</p>

<p>are equivalent.</p>

<p>The <em>implicit link name</em> shortcut allows you to omit the name of the
link, in which case the link text itself is used as the name.
Just use an empty set of square brackets – e.g., to link the word
“Google” to the google.com web site, you could simply write:</p>

<p><a href="http://google.com/">Google</a></p>

<p>And then define the link:</p>

<p>Because link names may contain spaces, this shortcut even works for
multiple words in the link text:</p>

<p>Visit <a href="http://daringfireball.net/">Daring Fireball</a> for more information.</p>

<p>And then define the link:</p>

<p>Link definitions can be placed anywhere in your Markdown document. I
tend to put them immediately after each paragraph in which they’re
used, but if you want, you can put them all at the end of your
document, sort of like footnotes.</p>

<p>Here’s an example of reference links in action:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>I get 10 times more traffic from [Google] [1] than from
[Yahoo] [2] or [MSN] [3].

  [1]: http://google.com/        "Google"
  [2]: http://search.yahoo.com/  "Yahoo Search"
  [3]: http://search.msn.com/    "MSN Search"
</code></pre></div></div>

<p>Using the implicit link name shortcut, you could instead write:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>I get 10 times more traffic from [Google][] than from
[Yahoo][] or [MSN][].

  [google]: http://google.com/        "Google"
  [yahoo]:  http://search.yahoo.com/  "Yahoo Search"
  [msn]:    http://search.msn.com/    "MSN Search"
</code></pre></div></div>

<p>Both of the above examples will produce the following <abbr title="Hyper Text Markup Language">HTML</abbr> output:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;I get 10 times more traffic from &lt;a href="http://google.com/"
title="Google"&gt;Google&lt;/a&gt; than from
&lt;a href="http://search.yahoo.com/" title="Yahoo Search"&gt;Yahoo&lt;/a&gt;
or &lt;a href="http://search.msn.com/" title="MSN Search"&gt;MSN&lt;/a&gt;.&lt;/p&gt;
</code></pre></div></div>

<p>For comparison, here is the same paragraph written using
Markdown’s inline link style:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>I get 10 times more traffic from [Google](http://google.com/ "Google")
than from [Yahoo](http://search.yahoo.com/ "Yahoo Search") or
[MSN](http://search.msn.com/ "MSN Search").
</code></pre></div></div>

<p>The point of reference-style links is not that they’re easier to
write. The point is that with reference-style links, your document
source is vastly more readable. Compare the above examples: using
reference-style links, the paragraph itself is only 81 characters
long; with inline-style links, it’s 176 characters; and as raw <abbr title="Hyper Text Markup Language">HTML</abbr>,
it’s 234 characters. In the raw <abbr title="Hyper Text Markup Language">HTML</abbr>, there’s more markup than there
is text.</p>

<p>With Markdown’s reference-style links, a source document much more
closely resembles the final output, as rendered in a browser. By
allowing you to move the markup-related metadata out of the paragraph,
you can add links without interrupting the narrative flow of your
prose.</p>

<h3 id="emphasis">Emphasis</h3>

<p>Markdown treats asterisks (<code class="language-plaintext highlighter-rouge">*</code>) and underscores (<code class="language-plaintext highlighter-rouge">_</code>) as indicators of
emphasis. Text wrapped with one <code class="language-plaintext highlighter-rouge">*</code> or <code class="language-plaintext highlighter-rouge">_</code> will be wrapped with an
<abbr title="Hyper Text Markup Language">HTML</abbr> <code class="language-plaintext highlighter-rouge">&lt;em&gt;</code> tag; double <code class="language-plaintext highlighter-rouge">*</code>’s or <code class="language-plaintext highlighter-rouge">_</code>’s will be wrapped with an <abbr title="Hyper Text Markup Language">HTML</abbr>
<code class="language-plaintext highlighter-rouge">&lt;strong&gt;</code> tag. E.g., this input:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*single asterisks*

_single underscores_

**double asterisks**

__double underscores__
</code></pre></div></div>

<p>will produce:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;em&gt;single asterisks&lt;/em&gt;

&lt;em&gt;single underscores&lt;/em&gt;

&lt;strong&gt;double asterisks&lt;/strong&gt;

&lt;strong&gt;double underscores&lt;/strong&gt;
</code></pre></div></div>

<p>You can use whichever style you prefer; the lone restriction is that
the same character must be used to open and close an emphasis span.</p>

<p>Emphasis can be used in the middle of a word:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>un*frigging*believable
</code></pre></div></div>

<p>But if you surround an <code class="language-plaintext highlighter-rouge">*</code> or <code class="language-plaintext highlighter-rouge">_</code> with spaces, it’ll be treated as a
literal asterisk or underscore.</p>

<p>To produce a literal asterisk or underscore at a position where it
would otherwise be used as an emphasis delimiter, you can backslash
escape it:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\*this text is surrounded by literal asterisks\*
</code></pre></div></div>

<h3 id="code">Code</h3>

<p>To indicate a span of code, wrap it with backtick quotes (<code class="language-plaintext highlighter-rouge">`</code>).
Unlike a pre-formatted code block, a code span indicates code within a
normal paragraph. For example:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use the `printf()` function.
</code></pre></div></div>

<p>will produce:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;Use the &lt;code&gt;printf()&lt;/code&gt; function.&lt;/p&gt;
</code></pre></div></div>

<p>To include a literal backtick character within a code span, you can use
multiple backticks as the opening and closing delimiters:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>``There is a literal backtick (`) here.``
</code></pre></div></div>

<p>which will produce this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;&lt;code&gt;There is a literal backtick (`) here.&lt;/code&gt;&lt;/p&gt;
</code></pre></div></div>

<p>The backtick delimiters surrounding a code span may include spaces –
one after the opening, one before the closing. This allows you to place
literal backtick characters at the beginning or end of a code span:</p>

<p>A single backtick in a code span: <code class="language-plaintext highlighter-rouge">`</code></p>

<p>A backtick-delimited string in a code span: <code class="language-plaintext highlighter-rouge">`foo`</code></p>

<p>will produce:</p>

<p>A single backtick in a code span: <code>`</code></p>

<p>A backtick-delimited string in a code span: <code>`foo`</code></p>

<p>With a code span, ampersands and angle brackets are encoded as <abbr title="Hyper Text Markup Language">HTML</abbr>
entities automatically, which makes it easy to include example <abbr title="Hyper Text Markup Language">HTML</abbr>
tags. Markdown will turn this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Please don't use any `&lt;blink&gt;` tags.
</code></pre></div></div>

<p>into:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;Please don't use any &lt;code&gt;&amp;lt;blink&amp;gt;&lt;/code&gt; tags.&lt;/p&gt;
</code></pre></div></div>

<p>You can write this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>`&amp;#8212;` is the decimal-encoded equivalent of `&amp;mdash;`.
</code></pre></div></div>

<p>to produce:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;p&gt;&lt;code&gt;&amp;amp;#8212;&lt;/code&gt; is the decimal-encoded
equivalent of &lt;code&gt;&amp;amp;mdash;&lt;/code&gt;.&lt;/p&gt;
</code></pre></div></div>

<h3 id="images">Images</h3>

<p>Admittedly, it’s fairly difficult to devise a “natural” syntax for
placing images into a plain text document format.</p>

<p>Markdown uses an image syntax that is intended to resemble the syntax
for links, allowing for two styles: <em>inline</em> and <em>reference</em>.</p>

<p>Inline image syntax looks like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>![Alt text](/path/to/img.jpg)

![Alt text](/path/to/img.jpg "Optional title")
</code></pre></div></div>

<p>That is:</p>

<ul>
  <li>An exclamation mark: <code class="language-plaintext highlighter-rouge">!</code>;</li>
  <li>followed by a set of square brackets, containing the <code class="language-plaintext highlighter-rouge">alt</code>
attribute text for the image;</li>
  <li>followed by a set of parentheses, containing the URL or path to
the image, and an optional <code class="language-plaintext highlighter-rouge">title</code> attribute enclosed in double
or single quotes.</li>
</ul>

<p>Reference-style image syntax looks like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>![Alt text][id]
</code></pre></div></div>

<p>Where “id” is the name of a defined image reference. Image references
are defined using syntax identical to link references:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[id]: url/to/image  "Optional title attribute"
</code></pre></div></div>

<p>As of this writing, Markdown has no syntax for specifying the
dimensions of an image; if this is important to you, you can simply
use regular <abbr title="Hyper Text Markup Language">HTML</abbr> <code class="language-plaintext highlighter-rouge">&lt;img&gt;</code> tags.</p>

<h2 id="miscellaneous">Miscellaneous</h2>

<h3 id="automatic-links">Automatic Links</h3>

<p>Markdown supports a shortcut style for creating “automatic” links for URLs and email addresses: simply surround the URL or email address with angle brackets. What this means is that if you want to show the actual text of a URL or email address, and also have it be a clickable link, you can do this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;http://example.com/&gt;
</code></pre></div></div>

<p>Markdown will turn this into:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;a href="http://example.com/"&gt;http://example.com/&lt;/a&gt;
</code></pre></div></div>

<p>Automatic links for email addresses work similarly, except that
Markdown will also perform a bit of randomized decimal and hex
entity-encoding to help obscure your address from address-harvesting
spambots. For example, Markdown will turn this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;address@example.com&gt;
</code></pre></div></div>

<p>into something like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;a href="&amp;#x6D;&amp;#x61;i&amp;#x6C;&amp;#x74;&amp;#x6F;:&amp;#x61;&amp;#x64;&amp;#x64;&amp;#x72;&amp;#x65;
&amp;#115;&amp;#115;&amp;#64;&amp;#101;&amp;#120;&amp;#x61;&amp;#109;&amp;#x70;&amp;#x6C;e&amp;#x2E;&amp;#99;&amp;#111;
&amp;#109;"&gt;&amp;#x61;&amp;#x64;&amp;#x64;&amp;#x72;&amp;#x65;&amp;#115;&amp;#115;&amp;#64;&amp;#101;&amp;#120;&amp;#x61;
&amp;#109;&amp;#x70;&amp;#x6C;e&amp;#x2E;&amp;#99;&amp;#111;&amp;#109;&lt;/a&gt;
</code></pre></div></div>

<p>which will render in a browser as a clickable link to “address@example.com”.</p>

<p>(This sort of entity-encoding trick will indeed fool many, if not
most, address-harvesting bots, but it definitely won’t fool all of
them. It’s better than nothing, but an address published in this way
will probably eventually start receiving spam.)</p>

<h3 id="backslash-escapes">Backslash Escapes</h3>

<p>Markdown allows you to use backslash escapes to generate literal
characters which would otherwise have special meaning in Markdown’s
formatting syntax. For example, if you wanted to surround a word
with literal asterisks (instead of an <abbr title="Hyper Text Markup Language">HTML</abbr> <code class="language-plaintext highlighter-rouge">&lt;em&gt;</code> tag), you can use
backslashes before the asterisks, like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\*literal asterisks\*
</code></pre></div></div>

<p>Markdown provides backslash escapes for the following characters:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\   backslash
`   backtick
*   asterisk
_   underscore
{}  curly braces
[]  square brackets
()  parentheses
#   hash mark   + plus sign   - minus sign (hyphen)
.   dot
!   exclamation mark
</code></pre></div></div>

<h2 id="extensions">Extensions</h2>

<h3 id="footnotes">Footnotes</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Here's a simple footnote,[^1] and here's a longer one.[^bignote]

[^1]: This is the first footnote.

[^bignote]: Here's one with multiple paragraphs and code.

    Indent paragraphs to include them in the footnote.

    `{ my code }`

    Add as many paragraphs as you like.
</code></pre></div></div>

<p>Here’s a simple footnote<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>, and here’s a longer one<sup id="fnref:bignote"><a href="#fn:bignote" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>.</p>

<h3 id="meta-data">Meta-Data</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Title:   My Document
Summary: A brief description of my document.
Authors: Waylan Limberg
         John Doe
Date:    October 2, 2007
blank-value:
base_url: http://example.com

This is the first paragraph of the document.
</code></pre></div></div>

<h3 id="abbreviations">Abbreviations</h3>

<p>The <abbr title="Hyper Text Markup Language">HTML</abbr> specification
is maintained by the <abbr title="World Wide Web Consortium">W3C</abbr>.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*[HTML]: Hyper Text Markup Language
*[W3C]:  World Wide Web Consortium
</code></pre></div></div>

<h3 id="definition-lists">Definition Lists</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Apple
:   Pomaceous fruit of plants of the genus Malus in
    the family Rosaceae.

Orange
:   The fruit of an evergreen tree of the genus Citrus.
</code></pre></div></div>

<dl>
  <dt>Apple</dt>
  <dd>Pomaceous fruit of plants of the genus Malus in
the family Rosaceae.</dd>
  <dt>Orange</dt>
  <dd>The fruit of an evergreen tree of the genus Citrus.</dd>
</dl>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>This is the first footnote. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:bignote">
      <p>Here’s one with multiple paragraphs and code.</p>

      <p>Indent paragraphs to include them in the footnote.</p>

      <p><code class="language-plaintext highlighter-rouge">{ my code }</code></p>

      <p>Add as many paragraphs as you like. <a href="#fnref:bignote" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="other" /><summary type="html"><![CDATA[Original page]]></summary></entry></feed>